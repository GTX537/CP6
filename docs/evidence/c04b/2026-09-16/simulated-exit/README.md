# C04B 模拟开发验收证据

用户于2026-09-16确认没有真实业务数据，批准将本轮C系列改为模拟开发验收。源码提交 `30ad1936eb0749b72dd0d1785ca95eaa4d154430`；Core基线 `1c68b9dc64f527285a0edd6d9cac81dcac1964c9`。五项权重仍各20%，本任务双仓库远端main包含性核实后从80%达到 **100%开发验收**。此归档发生于合并前，最终Git交付另留回执；不把生产验收称为完成。

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| 固定提交静态盘点 | 生产阻塞文件8→0 | 历史迁移2、运维/来源保护3、测试15个引用文件有意保留；不是动态调用证明 |
| 当前模型/程序集与迁移 | 4/4通过 | 20旧实体退出；C01/C02/C03模型保留；Up无表/数据操作，Down拒绝 |
| 真实SQL模拟升级 | 1/1通过，0跳过 | 独占新库、每个旧表1条模拟记录；20表列元数据/行数/内容摘要不变；不等同完整索引/FK指纹 |
| 当前身份/投影/ERP存储 | 同一SQL验收通过 | 两租户投影分离、ERP关联读写及跨租户不可见；非本次重跑登录/消息端到端 |
| 受影响历史CRM回归 | 17/17通过 | 保留原测试及历史夹具，不以它替代当前模型检查 |
| 盘点工具 | 16/16通过 | 提交绑定、精确匹配、隐私、输入失败关闭、拒绝覆盖；最终38项唯一测试全通过 |
| 本地发布和实际HTTP | 257文件；live/ready/OpenAPI均200 | 独立loopback端口、独占空SQL库；后台任务/初始化/CRM桥关闭，仅证明启动/连接/HTTP管道 |
| 清理 | 临时库0，自有API已停止 | 原采用宿主、原库、日历、备份及429文件运行包不替换 |

SQL夹具使用当前Core模型和原CrmFoundation表定义，写入明确模拟记录并构造前序迁移历史，再实际执行新退休标记迁移；只证明此次增量兼容，不声称全历史迁移重放。原20物理表与保护工具保留，物理DROP另行决定。

`before-exit.json`保留最初盘点原件及旧字段/工具摘要；`after-exit.json`使用最终工具报告，源码版本不同；不倒改旧结果。`acceptance-summary.json`绑定原TRX/红灯日志摘要，原始执行输出保存在本机审计目录。`manifest.json`固定此目录结果文件字节。`local-http.json`列出发布文件SHA和准确HTTP检查范围。

执行入口见Core `tools/crm-legacy-inventory/README.md`；受影响回归命令为 `dotnet test CP6.Tests/CP6.Tests.csproj -c Release --filter FullyQualifiedName~CP6.Tests.Crm.`。发布复用同一代码已编译结果：`dotnet publish CP6.WebApi/CP6.WebApi.csproj -c Release --no-build --no-restore`，独立目录启动。HTTP配置与清理结果在回执中，私有SQL连接串和JWT随机值不入库。

原C01–C03、五项来源/启用SQL、六库恢复和C04A真实本机采用结果复用，不重复运行。真实用户样本、10工作日/200条Lead、正式发布周期观察及生产推广留在独立生产验收；没有用此模拟结果替换历史真实证据。

集中审查修正：旧测试夹具的索引配置须先于Core共用租户转换，新增回归先准确失败，修正后受影响17项通过。原16项执行记录与红灯保留；最终测试夹具/盘点提交 `384a246a2edaeae0efcd38948adb8b7542ddb4c7`。生产输入相对上述原模型/SQL/发布检查提交未变，这三类成功结果直接复用；没有重跑SQL或再次发布。
