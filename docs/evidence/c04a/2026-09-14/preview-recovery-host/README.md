# C04A 独立恢复宿主开发验证

CRM 代码提交 `93da582661c707c190eb51f4992d3a9e0886d336` 实现独立 `Crm.LocalPreviewHost`。发布宿主仅有 .NET / ASP.NET 共享框架依赖，没有 CRM API、SQL、Dapr 客户端或平台包依赖。它复用私有原配置启动四个既有角色并提供三个路径的传输 relay，不调用数据库初始化或验收场景。

17 项新检查通过，无失败或跳过。16 项使用真实发布宿主 CLI，1 项使用同源宿主 API 的取消入口；子进程是自有临时 HTTP/Node 夹具，整个测试没有连接 SQL。覆盖只读检查/必要文件绑定、正常停启、回调认证头/正文/状态转发、预存停止标记、双宿主/存活 PID 拒绝、输入变化、启动失败、运行中退出和取消清理。没有重跑既有通过套件。

原共享预览清单只读检查识别到 18 个必要输入；输出仅为待审阅请求。六个原进程身份和八个入口/控制文件与上一任务完全一致。自有夹具进程和目录均为零；原私有配置和旧发布目录未修改。本次没有执行实际停止、启动、采用、迁移或数据库操作。

[验证记录](local-validation.json)、[准确发布清单](publication-manifest.json)、[审查范围](review.json)、[执行原件](local-execution.zip)。446 文件边界通过，Worker P08 与所有历史授权边界保留。该记录证明开发组件，正常 PR 与远端 main 包含性另记交付记录。

宿主必要文件绑定不是完整运行包或管理员隔离证明，也不是实际 CRM 0.2 启动恢复验收。来源证明/目标整组启用草稿 Core #114、CRM #75 的 SQL 门禁继续开放；保留其 62 项通过结果。下一步继续实际生命周期/特权控制、完整执行恢复包、当前字节签收及 CRM11/C04B 验收和观察期。总体 60%（3/5）。
