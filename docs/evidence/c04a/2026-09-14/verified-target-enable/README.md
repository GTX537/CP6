# C04A 来源冻结与整组启用：开发草稿

Core 已实现真实 Frozen 证明；CRM 已实现固定工具包验证、完整目标持锁及同实例两库的一次事务启用。代码提交：Core `7667b693e0264b13c36adc4e9f09331ceceb6dc5`，CRM `ff0bfbcfe05e0a4ce2883bd9d4fa36dcd5bc43f1`。当前为草稿，必要 SQL 门禁尚未通过，禁止合并或实际启用。

Core 49 项新输入/契约检查、CRM 13 项包清单/文件句柄检查通过，均无跳过。首轮三个新 SQL 场景在来源检查或清理登录阶段失败；单独重试一次抵达 `CRM_SOURCE_PROOF_REJECTED`，当时未保留内部 Core 代码，原因仍待定位。随后加入仅允许固定 Core 错误标识的诊断，下一次运行在 SQL 登录/清理超时。原失败日志全部保留，未放宽连接、权限或超时，未重跑既有无关通过套件。

当前还需通过三份来源/两目标成功与重放、来源保护失效与混合状态拒绝、第二目标运行复用导致整组回滚三个场景，以及因原 Enable/Close 操作抽取而受影响的两个旧场景。静态完整差异与 444 文件边界通过，不能替代 SQL 验证。

[验证状态](local-validation.json)、[发布清单](publication-manifest.json)、[审查范围](review.json)、[原始执行归档](local-execution.zip)。Core 40 文件、CRM 原版本 178 文件和增加错误标识后的 178 文件分别保留；13 项包检查属于原发布版本，后续变化未触及这些验证路径。

最终只读观察确认原生及 Docker 临时测试库均为 0，实际 CP6DB 没有 crm_source_control Schema。两个本轮失败夹具目录保留在原私有 Temp 位置，仅供调查，配置未收入归档。现有共享预览的 6 个进程身份及 8 个入口/控制文件与上一交付一致。

最近 SQL 进程报告 physical-memory-low，本机可用内存约 1.1 GiB；只读连接曾恢复，但运行期间仍发生登录超时。该观察不足以证明全部失败原因。恢复后使用原发布包仅运行未通过项；保留这 62 项通过结果及其版本范围。具体实际启动入口/特权控制、恢复宿主兼容、最终执行包、0.2 字节签收和 CRM11/C04B 正式验收/观察期继续开放。总体 60%（3/5）。
