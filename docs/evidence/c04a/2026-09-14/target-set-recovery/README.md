# C04A 多目标恢复开发证据

多目标恢复的必要开发检查已全部通过：60 项新增检查、2 项直接受影响检查，无跳过。来源冻结绑定完整目标集合；所有目标均永久关闭、且每份许可均包含该来源后，Core 持有全部目标锁执行来源恢复。单目标入口不能降级恢复集合绑定来源。

最终记录为[完成验证](completion-validation.json)、[补充执行归档](completion-execution.zip)和[组合发布绑定](combined-publication-manifest.json)。此前[草稿验证](local-validation.json)、[原始执行归档](local-execution.zip)、[原发布绑定](publication-manifest.json)及[集中审查](review.json)保留历史范围，其中旧的 pending/mergeAllowed=false 表示当时状态，由完成验证明确关闭，不改写失败原件。

最后一项单目标恢复检查在新的只读 SQL 连接成功后，以原样发布二进制通过，没有改产品、配置或超时。此前观察到内存压力，但各次超时精确根因未确定。原生和 Docker 实例的临时测试库、实际来源控制 Schema 最终均为零。此前自动审批拒绝删除一个自有失败夹具目录（blocked by policy），该目录仍保留；SQL 数据库已单独清理。

草稿已同步最新 main 的已验证启动控制。Core 代码未变，40 个发布文件原样沿用；CRM 组合版本从合并提交重新构建并固定 178 个文件。目标集合实现、SQL 门禁和测试源码保持原字节；启动控制保持已交付字节。合并后的帮助和两项缺参拒绝验证了命令入口组合，没有重跑已通过套件或把旧检查标为新二进制运行。438 文件边界通过，冲突解决及组合差异审查无待修复项。正常 PR 合并与远端 main 包含性另记本机交付记录。

这完成多目标恢复组件，不是实际切换验收。实际来源、目标及共享预览未切换；恢复宿主依赖兼容、特权写入控制、来源冻结与目标启用联动、最终执行批准包仍待完成。0.2 字节未签收，CRM11/C04B 正式验收和观察期保留，总体 **60%（3/5）**。
