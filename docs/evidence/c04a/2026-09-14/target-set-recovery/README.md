# C04A 多目标恢复开发证据（草稿）

[完整目标集合恢复](../../../../../tools/CP6.Crm.SourceFence/ACTUAL-RECOVERY-TARGETS.md)的实现已保存为 Core 功能提交 `0ca9e973ffb1766c46ba90d1ecfae33186e58432` 和 CRM 功能提交 `8ca2f01554705527e53b570ed864f81e8a30fb7e`；仍有一项必要检查未通过，当前只供草稿审阅，不可合并。来源在冻结前绑定完整集合，所有目标均永久关闭且许可均包含来源后，Core 才持有全部目标锁执行来源恢复。原单目标入口不能降级恢复集合绑定来源。

| 验证 | 结果 |
| --- | --- |
| 新集合契约及严格目标文件/CLI 输入 | 56/56 |
| 新原生/Docker 发布 CLI 集合恢复、全锁、Enabled/Written、来源成员拒绝 | 4/4 分阶段通过 |
| 直接受影响来源 DDL 原子性 | 1/1 |
| 直接受影响单目标持锁恢复 | 0/1，超时，待完成 |
| 跳过项 | 0 |
| CRM02 授权边界 | 438 文件通过 |

初次跨仓运行的两项失败发生在原生来源 Freeze 阶段；同一发布版本定向复验通过，没有改代码或放宽超时。两项受影响旧检查首次运行遇到原生握手/清理超时；随后来源 DDL 检查通过，单目标持锁恢复仍在状态读取阶段返回 `C04A_LOCK_TIMEOUT`，没有完成后续恢复断言。原始失败日志全部保留，不标为通过或跳过。

SQL 观察到物理内存压力，主机可用内存约 1 GiB，之后只读连接仍握手超时；这说明存在资源压力，尚不能确定每次超时的根因。已按准确自有名称清理先前 4 个残留库；最终清理观察尚未取得，不能宣称全部无残留。自动审批拒绝递归删除一个自有失败夹具临时目录（`blocked by policy`），该目录保留。没有修改产品、配置、超时或共享进程。一次集中代码审查没有待修复项，新 Core 40 文件与 CRM 178 文件已固定摘要。

[验证清单](local-validation.json)、[执行归档](local-execution.zip)、[发布与源码绑定](publication-manifest.json)、[审查结论](review.json)记录各阶段适用版本。56 项输入/契约结果保留首次二进制范围；随后两条实时租约复核已进入最终发布 CLI 验证，不把旧结果标为新二进制重跑。

当前多组织恢复组件尚未交付；剩余必要检查通过并正常合并后，另记远端 main 包含性。实际来源/目标/共享预览未切换；启动入口与特权控制、前向启用、完整具体执行批准包仍待完成。0.2 字节未签收，CRM11/C04B 正式验收和观察期保留，总体 **60%（3/5）**。
