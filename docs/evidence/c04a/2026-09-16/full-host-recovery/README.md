# 原密钥、完整恢复宿主与身份恢复验证

本次用既有真实 C02 备份和已完成初始化的隔离副本验证 A09。复用[六库保障](../sixth-db-restore/README.md)及[一次前向初始化](../../2026-09-15/restored-initialization/README.md)，没有重跑原五项 SQL、前五库检查或初始化。原实际环境未切换。

## 已取得的结果

| 验证 | 结果 |
| --- | --- |
| 原 CRM keyring 解密历史数据 | 两组织 8 条已有加密会话全部解密；禁用自动生成密钥，前后摘要一致 |
| 过期会话边界 | 8 条均已过期，仅用于密钥恢复证明；登录重新走原账号密码与 OIDC |
| 完整恢复宿主 | Core、CRM、Web、dispatcher 及宿主五进程存活，relay 归属正确 |
| 隔离 transport | 新建私有 Kafka / 两 Dapr 容器，Kafka 健康，Dapr metadata 200；原 C02 transport 未启动 |
| 两组织身份对账 | Ready，两组织合计 119 条投影、105 个发布用户，0 漂移、0 对账失败 |
| 真实网页登录 | 两组织原有管理员均通过密码登录、OIDC/PKCE 回调和会话身份核对；没有新建账号 |
| 已认证读取与组织绑定 | 两组织 workspace / Lead 列表均为 200；外来组织请求头不能覆盖会话组织 |
| 目标保护状态 | 两个业务副本始终 Closed / Generation 0 / READ_ONLY；live 200、ready 503 |
| 公开表单 | 两组织均 409；确认 `crm.configuration_not_active`，初始日历仍须 A10 处理，不算表单可用 |
| 结束后的保留 | 宿主与自有子进程正常退出；三个私有 transport 停止并保留；四个数据库只读保留 |
| 原件保持 | 原 11 份私有输入、206 份 API 与 5 份恢复宿主发布文件、原 51 个容器身份/配置/挂载/策略及状态均保持 |

认证副本先另做不覆盖的 COPY_ONLY/CHECKSUM 备份和 VERIFYONLY，才临时开放该副本的会话/身份写入；两业务副本没有开放写入。Core 从已验证备份新建恢复副本。认证副本现包含本次对账/登录结果，其运行前初始化状态另有备份；不能把当前副本当成未运行过的旧快照。

## 审计与复现

[汇总与执行原件哈希](recovery-summary.json)、[原密钥证明](key-recovery.json)、[两个组织登录读取](restored-login.json)、[宿主与身份状态](runtime-proof.json)、[退出和保留](retention.json)。探针源码、运行脚本、请求摘要和失败记录保留在 `D:/CP6-audit-results/20260916/full-host-recovery/`；受限输入和日志位于该目录 preparation.json 指向的私有根，归档 SHA-256 见汇总。公开材料没有配置原值、口令、密钥、令牌或解密内容。

运行顺序为：原备份恢复 Core → 复制原配置与 keyring 并绑定隔离连接 → 禁止生成新密钥的旧载荷解密 → 保留已初始化认证副本 → 独立宿主 inspect/run → 两组织原生密码与 OIDC 登录/读取 → 记录对账及存活进程 → stop-preview → 停止精确自有 transport → 四副本 READ_ONLY。重新执行须另建独立任务和归属目录，不直接重跑本次一次性准备脚本。

首次启动的 dispatcher 拒绝自建副本名称，宿主按设计退出所有子进程。只将新 Core 副本及 content-root 改为既有 dispatcher 要求的 `CP6C02Live_<32 hex>` 后成功；没有放宽安全校验或重复初始化。原失败请求/日志保留。一次 SQL 登录超时随后在只读检查中恢复，没有修改系统/SQL 内存设置。新增本机探针使用发布包对应的 Windows 运行时程序集，最终构建零警告零错误。

## 完成边界

A09 完成，C04A 为 9/12＝75.0%，整体清单 75.0%（上次 73.3%，精确增量 1.6667 个百分点），正式里程碑仍 60%。这证明真实恢复副本上的密钥、完整宿主与身份兼容；不代表原实际库初始化、来源冻结、目标启用、正式切换、CRM11 或 C04B 验收，也没有重复 C02 事件交付测试。日历初始版本、独占写入控制/最终签收和实际切换仍开放。
