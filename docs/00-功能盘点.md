# CP6 功能盘点 + 迈向完整 ERP / 可售 SaaS 的完善路线

> **生成于 2026-06-13；最近更新 2026-06-21。** 本文做三件事：①盘点**现有功能**（已实现并运行）②梳理**已规划新增功能**③深挖：要成为**完整 ERP / 可售 SaaS**，**还缺什么、怎么补**。
>
> **2026-06-21 进度快照**：原 6-13 列为"已规划待编码"的七大块（PUB 权限组织 / 财务会计 / 采购 / OA 审批 / 多租户 / i18n）**已全部全栈落地上 main**；ERP 完整性 **A 类五缺口（A1 MRP / A2 工艺路线 / A3 固定资产 / A4 银行对账 / A5 预算）全部收官**——**单租户完整制造业 ERP 已达成**。本地 `main` 与 `origin/main` 已同步（全部 push）。下文已把这些从"待编码/缺口"上移为"已实现"。
>
> 定位前提（见 `docs/00-product-blueprint.md` 与项目记忆）：**深耕纸箱行业、做完整可售产品**。故下面的"缺口"按"纸箱厂可售 ERP/SaaS"口径评估，通用 ERP 里与本行业弱相关的模块（如全套 HR 薪资、全套 CRM 营销）标为低优先或不做。
>
> 现有功能基于真实代码盘点（2026-06-21）：`*Controller.cs`（**115 个**）+ `cp6.web/src/views`（**155 页面**）+ **14 个**后台服务；测试 **927 绿 + 1 skip**。

---

## 一、现有功能（已实现并运行）

CP6 当前已是一套**完整核心制造业 ERP**：販売→生産→在庫正向链 + ERP→MES→WMS 闭环全通，再叠加**采购链 / 财务会计 / OA 审批 / PUB 权限组织 / 多租户 / i18n**，以及 **ERP 完整性 A 类五缺口（MRP / 工艺路线 / 固定资产 / 银行对账 / 预算）**。下分两段：**§1.1–1.5 为最早的进销存+MES 地基**，**§1.6 为本季度新增的中台与完整性模块**。

### 1. 販売管理 MSBB（ERP·15 控制器）
取引先 `BusinessPartner`（9 属性多 Tab：得意先/発注先/買掛先…）· 製品 `Product`（5 步向导）· 見積 `Quotation` + 見積計算 `EstimateCalc`(3 步) · 受注 `Order`（3 步向导 + 取消级联 + 价格修正 + 订单追溯）· 未出荷 `Backorder`/`UnshippedOrder` · 信用单 `CreditNote` · 為替 `FxRate`（多币种）· 印版木型 `PlateMold` · シート単価 `SheetUnitPrice` · FSC 检查表 · OTD（准时交付）报表 · 主数据

### 2. 生産管理 MES（9 控制器）
製造指図 `WorkOrder`(3 步向导) · 生産実績 `ProductionResult` · 品質検査 `QualityInspection` · 不良記録 `DefectRecord` · 設備 `Machine` + 停机监控 · OEE 分析（后台自动算）· 計画ボード `PlanningBoard` · 達成率 `PlanAchievement` · MES 看板 / Control Tower

### 3. 在庫物流 WMS（~35 控制器，体量最厚）
倉庫/库位 · 在庫 `Stock` · 入庫 `Inbound`(予定/実績) · 出庫 `Outbound` + 多倉庫引当 `OutboundRouting` · 拣货/梱包出荷 · 棚卸 `StockTake` · 配送 `Carrier` · 补充 `Replenish` · 仓位优化 `Slotting` · 跨库 `CrossDock` · キット `Kitting` · 返品 `RMA` · 検品 `QcInspection` · **纸器业特化**：原紙卷 `PaperRoll`、インキ `InkLot`、パレット、残材 `Remnant`、印版在庫、サンプル、VMI · 期限/ロット追跡/滞留/欠品 `MaterialShortage` · WCS/IoT 监控/移动作业 · 报表中心

### 4. 系统底座 → 已升级为 PUB 公共平台（强校验）
登录鉴权 `Auth`(JWT) · 字典 `Dict` · 多语言 `Lang`(5 语) · 操作日志 `OperLog`(Kafka 异步 + 定时清理) · Dashboard。**原"单角色 + 仅前端隐藏菜单、无后端强校验"的薄权限层已被 PUB 取代**（见 §1.6）：四粒度后端强校验 + 组织架构 `Sys_Dept`。

### 5. 集成与闭环（Phase 6-10）
ERP→MES→WMS 闭环（Bridge Hook Phase1-4）· IntegrationEvent 持久化 + 重试 Worker + 死信告警 · 集成健康看板 · 后台服务：Kafka 日志 / 设备监控 / 通知 / OEE 计算 / 日志清理

### 6. 新增中台与完整性模块（本季度全栈落地·6-21）
原 6-13 列为"已规划待编码"，现**已全部上 main、经 gstack 真浏览器 QA**：

| 模块 | 已实现内容 | 里程碑测试数 |
|---|---|---|
| **PUB 公共平台**（408 测试） | 组织 `Sys_Dept` + 权限引擎**四粒度后端强校验** + 公共模组（附件/导入导出/代码生成） | 408 |
| **财务会计**（566 测试） | GL 总账 + AP/AR + 三大报表 + 成本会计 + 内控审计轨迹（深耕纸箱国别模板） | 566 |
| **OA 审批引擎**（631 测试） | 审批引擎阶 1~4（低代码表单/流程/规则/设计器）+ **多租户基建 `TenantId` 行级隔离** | 631 |
| **采购**（章01~09 全栈） | 主数据/PO/GR/三单匹配自动建 AP + PR(章05) + RFQ 比价(章06) + **外注加工(章07：支給材发料/成品成本/防吞料对账)** + 采购对账防虚开吞料(章08/09)。**外注两委托已接真实**（WMS 出库扣库存 + 财务06成本入账借FG贷INVENTORY，6-21） | 755→ |
| **多语言 i18n** | 三铁律 + 窄表 + `TenantId` + `DbStringLocalizer`；硬编码归零，词条 3040 | P1~P5 |
| **A1 MRP 净需求**（790 测试） | `Plan` 模块：受注/预测 × BOM 展开 − 库存 − 在途 − 在制 → 建议采购/工单（低层码引擎） | 790 |
| **A2 工艺路线完善**（807 测试） | 标准工时 × 工序费率 → 成本"做真"+ 工时采集/差异 + CRP 地基 | 807 |
| **A3 固定资产** | 资产卡片 + 四法折旧 + 全套处置 + 月末折旧 Worker → 自动凭证（复用引擎） | — |
| **A4 银行对账**（879 测试） | 流水导入 + 流水↔银行 GL 自动撮合 + 双向调节表 + 锁后守卫 | 879 |
| **A5 预算/管理会计**（919 测试 +1 skip） | 多维年度预算 + 按月分解 + OA 审批 + 预算 vs 实际 + 可选过账控制（BudgetGuard） | 919 +1skip |

> **核心 ERP 七大块（销售/生产/库存/采购/财务/审批/权限组织）+ 多租户 + i18n + A 类五完整性缺口全部落地**——CP6 已从"进销存+MES"跨入**完整核心 ERP**。剩 Space 3D 底座（丛书定稿，未编码）见 §二。

---

## 二、仍在规划 / 未编码的新增功能

| 模块 | 性质 | 文档 | 状态 | 补的能力 |
|---|---|---|---|---|
| **Space 3D 空间数字底座** | 新增·商用 | 丛书 00~09 定稿 | 📝 待写 P1 计划 | 3D 仓库建模 + 可配置库位编码 + 实时库存叠加 |
| **OA 低代码引擎（商业化深化）** | 新增·商用 | 丛书写完 | 📝 待落地 | 在已上线审批引擎之上做完整低代码平台 |
| **MRP 套件 P2/P3/P4** | A1 后续 | — | 📝 待出计划 | 预测 / MPS / CRP（P4 CRP 前置 A2 已就绪） |
| **MSBB 详细规格逆向** | 既存·文档化 | 060 完成 | ⏳ 剩 22 本 | 既存販売管理日文設計書逆向（需原始設計書源） |

---

## 三、迈向「完整 ERP」还缺什么（已规划之外的 gap）

核心闭环 + A 类五缺口均已落地。对照"完整制造业 ERP"，**A 类已清零，剩 B/C 类缺口**。按对纸箱厂的价值排序：

### A. 高价值 · ✅ **全部已完成（2026-06-20~21 全栈上 main，gstack QA 过）**

> 原列为五大缺口，现已**全部收官 = 单租户 ERP 完整性达成**。从"记录业务"升级到"计划 + 控制业务"（MRP 排产备料、预算管控、资产折旧、银行对账）。

| # | 原缺口 | 已实现 | 落地数据 |
|---|---|---|---|
| **A1** ✅ | MRP / 物料需求计划 | `Plan` 新模块：受注/预测 × BOM 展开 − 库存 − 在途 − 在制 → 建议采购/生产（低层码净需求引擎 + 计划看板 + 人确认转 PR/工单） | 790 测试 |
| **A2** ✅ | 多级 BOM + 工艺路线 Routing | 成本"做真"：工序级标准工时 × 工序费率 + 工时采集/差异；多级 BOM 展开内核（A1 已含）+ CRP 地基 | 807 测试 |
| **A3** ✅ | 固定资产（卡片 + 折旧计提） | `FixedAsset`/`DepreciationSchedule`/`AssetDisposal` + 四法折旧 + 全套处置 + 月末折旧 Worker → 自动凭证（复用引擎） | — |
| **A4** ✅ | 资金/银行对账 | `BankStatement`/`BankTransaction` 流水导入 + 流水↔银行 GL 自动撮合 + 双向调节表 + 锁后守卫 | 879 测试 |
| **A5** ✅ | 预算管理 / 管理会计 | `Budget`/`BudgetVersion`/`BudgetLine`（科目×成本中心×期间）多维预算 + 按月分解 + OA 审批 + 预算 vs 实际 + 可选过账控制（BudgetGuard） | 919 测试 +1skip |

> 详见 `docs/00-ERP完整性路线.md`（已同步标记 ✅）。**剩余制造业 ERP 完整性 gap 仅 B/C 类（中低价值，按客户拉动）。**

### B. 中价值 · 看客户需求补

| # | 缺口 | 说明 |
|---|---|---|
| **B1** | **现金流量表 + 合并报表** | 财务 08 章把现金流量表延后到 v2；多法人合并报表明确暂不做（单企业纸箱厂）。多客户后按需补 |
| **B2** | **质量管理 QMS 进阶** | 已有 QC 检品 + 不良记录，缺 **SPC 统计过程控制 / 不合格品处理(CAPA) / 来料-过程-出货全链质量追溯闭环 / 质量证书** |
| **B3** | **设备维护 EAM/TPM** | 已有设备 + 停机 + OEE，缺**保养计划 / 点检 / 备件 / 维修工单**（从"监控设备"到"管理设备生命周期"） |
| **B4** | **销售进阶（轻 CRM + 合同）** | MSBB 以受注为中心，缺**销售机会/报价跟进/合同管理/客户信用台账**（信用额度在采购计划里提过，可延伸） |
| **B5** | **需求预测** | MRP（A1）的上游。纸箱多为订单驱动，预测优先级中等，但做 VMI/备库客户需要 |
| **B6** | **自定义报表 / BI 分析** | 各模块有 Dashboard + ReportCenter，但缺**跨模块自定义报表/钻取/经营分析看板**（PUB 代码生成 + 导入导出是基础，可在其上做报表设计器） |

### C. 低价值 / 本行业可不做

- **全套 HR / 薪资 / 考勤**（PUB 组织模型 `Sys_Dept` 够审批/数据权限用；全 HR 与纸箱主业弱相关，除非客户要）
- **全套营销 CRM / 电商**（深耕 B2B 纸箱，营销自动化非核心）
- **生产高级排程 APS**（PlanningBoard 够用；APS 是 MRP 之后的进阶，量大客户再说）

---

## 四、迈向「可售 SaaS」还缺什么（产品化 gap）

"完整 ERP"是**功能**完整；"可售 SaaS"还要**产品化 + 运营化**。多租户（已规划）是地基，但只是起点：

### A. 商业化必备（卖出去 + 收到钱）

| # | 缺口 | 说明 | 现有可借力 |
|---|---|---|---|
| **S1** | **租户自助开通 + 订阅计费** | OA 章10 提了"模板市场/按租户计费"概念，但缺**真正的计费引擎**：套餐/用量计量(用户数/单据数/存储)/账单/在线支付/试用转付费 | 多租户 `TenantId` 计量基础 |
| **S2** | **租户初始化/引导(Onboarding)** | 新租户开通 → 一键导入默认科目表/模板/字典/演示数据 → 配置向导。开通体验决定转化 | PUB 导入导出/codegen、财务多国别模板、OA 模板复制 |
| **S3** | **租户级配置/品牌定制** | 自定义字段、自定义表单/流程（OA 设计器有）、Logo/主题白标、自定义编码规则。让一套代码适配不同客户 | PUB codegen + OA 设计器 + Space 编码引擎 |

### B. 安全合规必备（企业客户的门槛）

| # | 缺口 | 说明 |
|---|---|---|
| **S4** | **SSO / OAuth / 2FA** | 现 JWT 登录，企业客户要单点登录(SAML/OIDC)、双因素认证、密码策略 |
| **S5** | **数据隔离与合规审计** | 多租户行级隔离(已规划) + 财务审计轨迹(已规划) 是基础；还需**租户数据导出/删除(GDPR)、跨租户访问审计、数据加密(传输+静态)** |
| **S6** | **细粒度审计/操作回放** | OperLog 有；SaaS 要能向客户证明"谁在何时改了什么"，财务 10 章的不可篡改+对账是范本，需推广到全模块 |

### C. 运营/可靠性必备（多客户跑起来不出事）

| # | 缺口 | 说明 |
|---|---|---|
| **S7** | **可观测性/监控告警** | 有 IntegrationEvent 健康 + 死信告警；SaaS 要**全栈监控(APM/日志/指标/链路)、租户级 SLA、容量预警**。已有 `/metrics`(Prometheus)、SignalR 是起点 |
| **S8** | **多租户性能/伸缩** | 共享表行级隔离下，热点租户隔离、查询性能(全表加 TenantId 索引)、分库分表预案 |
| **S9** | **备份/容灾/数据迁移** | 租户级备份恢复、版本升级不停机、租户数据搬迁 |
| **S10** | **开放能力：API / Webhook / EDI** | SaaS 要让客户系统对接：**开放 API + API Key/限流、Webhook 事件订阅、与客户/供应商的 EDI**（纸箱厂常与大客户做 EDI 下单/对账） |

### D. 体验/支撑

- **客户门户 / 供应商门户**（客户自助下单查单、供应商自助报价收 PO——RFQ 已有内部侧，外部门户可延伸）
- **移动端**（WMS 已有移动作业；审批/看板移动化）
- **帮助中心 / 应用内引导 / 在线文档**（产品化的软实力）

---

## 五、完善路线（四波 · 前两波已完成）

**原则**：核心 ERP 闭环 + 完整性 A 类（前两波）已落地；往下按"SaaS 产品化 → 行业纵深"推进。**不要在核心闭环未通时提前做 SaaS 计费/门户**（已满足，可往下走）。

### ✅ 第一波 · 完成核心 ERP 闭环 —— **已完成**
PUB B0 组织 → PUB B1 权限 → OA 审批 → 财务 + 采购（并行）→ 多租户（OA 章10 系统级收口）全部落地。
> 完成标志达成：销售→生产→库存→采购→财务全闭环，后端四粒度强校验，单租户可演示完整 ERP。

### ✅ 第二波 · 补"完整 ERP"高价值缺口（第三章 A 类）—— **已完成**
实际执行序：A1 MRP（最高价值+码就绪先做）→ A2 工艺路线 → A4 银行对账 → A3 固定资产 → A5 预算，五项全栈上 main。
> 完成标志达成：从"记录业务"升级到"计划+控制业务"（MRP 排产备料、预算管控、资产折旧、银行对账）。

### 👉 第三波 · 产品化为"可售 SaaS"（第四章）—— **下一阶段候选**
顺序建议：**S4/S5/S6 安全合规**（企业客户门槛，先过）→ **S2 Onboarding + S3 租户定制**（开通体验）→ **S1 订阅计费**（变现）→ **S7-S9 运营可靠性** → **S10 开放 API/EDI + 门户**。
> 完成标志：客户能自助开通、安全合规、按用量付费、稳定多租户运营。

### 第四波 · 行业纵深 / 差异化（按客户拉动）
B 类（QMS 进阶 / EAM / 轻 CRM 合同 / BI）+ Space 3D 高级可视化 + 需求预测——这些是"卖点"而非"门槛"，按客户需求拉动做。

---

## 六、一句话总结（2026-06-21）

> CP6 现已是**完整核心制造业 ERP**：进销存+MES 地基 + 采购/财务/审批/权限组织/多租户/i18n 中台 + **ERP 完整性 A 类五缺口（MRP/工艺路线/固定资产/银行对账/预算）全部收官**——**单租户完整 ERP 已达成**。
> 往下两个方向：①**ERP 行业纵深**（第三章 B 类：QMS 进阶 / EAM 设备维护 / 轻 CRM 合同 / BI 自定义报表 / 现金流量表）；②**产品化为可售 SaaS**（第四章：安全合规 → Onboarding/租户定制 → 订阅计费 → 运营可观测 → 开放 API/EDI）。另有 Space 3D 与 OA 低代码引擎两个商业化新模块（丛书已定稿，待编码）。

---

*生成于 2026-06-13；2026-06-21 更新至真实进度。现有功能据真实代码盘点（115 控制器 / 155 页面 / 14 后台服务 / 919 测试 +1skip）；A 类五缺口已全部落地（详见 docs/00-ERP完整性路线.md）。第三章 B 类、第四章 SaaS gap 为对照通用制造业 ERP/SaaS 的差距分析（建议性，落地前需逐项 brainstorming 定需求）。配套：docs/00-product-blueprint.md（建设顺序）、docs/00-执行计划总盘.md。*
