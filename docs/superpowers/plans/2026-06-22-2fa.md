# 双因素认证（2FA）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在已加固的自研认证（#1）之上叠加 2FA——TOTP 主因子 + 邮件 OTP 回退 + 按租户强制策略 + 两步式 pending 半登录 + 自助/管理重置，全栈 + 五语 + gstack QA。

**Architecture:** 方案=在现有 `AuthController` 登录流插 2FA 分支；新增 7 个单一职责服务（`CP6.Core/Services/Sys/`）+ `Sys_User` 加 3 字段 + `Sys_Tenant` 加 1 字段 + pending 令牌（短命 JWT + `IDistributedCache` 一次性状态）+ 4 个 `/auth/2fa/*` 端点 + 自助/策略控制器 + 前端挑战/入会屏。命名空间 **Sys**，错误码 **E-SEC-011~019**。所有约束对齐 spec §1 现状锚点。

**Tech Stack:** .NET 8 + EF Core 8 / `Otp.NET`（新增，TOTP RFC6238）/ `IDistributedCache`（已配，pending+OTP 状态）/ `System.Net.Mail`（SMTP）/ xUnit + EF InMemory / Vue 3.5 + element-plus + vue-i18n + axios + `qrcode`（新增前端包）。源 spec：`docs/superpowers/specs/2026-06-22-2fa-design.md`。

---

## 关键既有约定（落码前必读，复核行号可能微移）

- **登录** `CP6.WebApi/Controllers/Sys/AuthController.cs`：`Login` 在 `_tenant.CurrentTenantId=user.TenantId`（约 L134）后、`BuildAccessToken`+`IssueAsync`+`_cookies.WriteAuthCookies`+返档案 前，插 2FA 分支（§T6）。私有 `BuildAccessToken(Sys_User,string jti,bool mustChange)`；`ClientIp`/`ClientUa` 私有属性；注入 `_context/_config/_perm/_tenant/_hasher/_policy/_login/_audit/_refresh/_blacklist/_cookies/_sec`。
- **JwtHelper** `CP6.Core/Utilities/JwtHelper.cs`：`GenerateToken(...,Guid? tenantId,string? jti,bool mustChangePassword)`。T5 加 `GeneratePendingToken`。
- **AuthCookieWriter** `CP6.Core/Services/Sys/AuthCookieWriter.cs`：常量 `AccessCookie/RefreshCookie/CsrfCookie/RefreshPath`；`WriteAuthCookies/ClearAuthCookies/static NewCsrfToken()`。T5 加 `cp6_2fa` 常量 + `WritePendingCookies/ClearPendingCookies`，`ClearAuthCookies` 加清 cp6_2fa。
- **锁定/审计** `ILoginSecurityService`(`EnsureNotLocked`抛`InvalidOperationException("E-SEC-002")`/`RecordFailureAsync`/`RecordSuccessAsync`)、`ISecurityAuditService.LogAsync(SecurityEventType,Guid? uid,string? userName,string? tenantCode,string? ip,string? ua,string? reason=null)`、`Sys_SecurityLog`、`SecurityEventType` 枚举现 1~8。
- **缓存** `IDistributedCache`（`CacheTokenBlacklistService` 范本：`SetStringAsync(key,val,new DistributedCacheEntryOptions{AbsoluteExpirationRelativeToNow=ttl})` / `GetStringAsync`）。
- **配置** `SecurityOptions`（根含 Password/Lockout/Token/Cookie/Csrf；`IOptions<SecurityOptions>` 注入）。
- **错误码**：Core 服务 `throw new InvalidOperationException("E-SEC-0xx")`，控制器 catch 转 `new BizException(code)`（∈`CP6.WebApi.Localization`）。当前用户 `(await _perm.GetAsync()).UserId`。
- **授权** `[RequirePermission(menuKey,action)]`（∈`CP6.Core.Auth`）；seed `Sys_MenuAction`+`Sys_RoleAction` 授 RoleId=1（Program.cs 仿 fin/授权加固块）。
- **i18n** 仿 `I18nSecScreenSeed`（`.Items` 接 Program.cs `.Concat` 链），五语；菜单名直接中文。
- **多租户** `BaseTenantEntity`；`Sys_Tenant`；登录期无租户上下文 → `IgnoreQueryFilters()` 跨租户读。`ITenantContext.CurrentTenantId` 可写。
- **测试基建** `TestHelper.CreateInMemoryContext()`；`Options.Create(new SecurityOptions{...})`；`IDistributedCache` 测试用 `new MemoryDistributedCache(Options.Create(new MemoryDistributedCacheOptions()))`。
- **前端** `cp6.web/src`：`api/http.ts`(withCredentials+CSRF头+401自动refresh)、登录态 `localStorage 'cp6_authed'`、`views/LoginView.vue`(`authApi.login`)、`router/index.ts`(守卫读 cp6_authed + standalone 静态路由 `meta.standalone`)、`api/sys/auth.ts`、视图放 `views/pms/`。

### 关键类型签名（跨 Task 一致，勿改名）
```csharp
public class TwoFactorOptions { public string Issuer{get;set;}="CP6"; public int CodeWindow{get;set;}=1; public int EmailOtpMinutes{get;set;}=5; public int EmailOtpLength{get;set;}=6; public int EmailResendCooldownSeconds{get;set;}=60; public int PendingTokenMinutes{get;set;}=5; public bool EmailFallbackEnabled{get;set;}=true; }
public interface ITotpService { string GenerateSecret(); string BuildOtpAuthUri(string secret,string userName); bool Verify(string secret,string code); }
public interface IEmailSender { Task SendAsync(string to,string subject,string body); }
public interface IPendingTokenStore { string Create(Guid userId,Guid tenantId,string purpose); (Guid userId,Guid tenantId,string purpose)? Get(string pendingJti); void Consume(string pendingJti); }  // IDistributedCache 后端
public interface ITwoFactorService {
    int ResolveTenantMode(Guid tenantId);
    bool IsChallengeRequired(Sys_User user,int tenantMode);
    bool MustEnroll(Sys_User user,int tenantMode);
    string BeginEnrollment(Sys_User user);                 // 已 Enabled 抛 E-SEC-017
    Task<bool> ConfirmEnrollmentAsync(Sys_User user,string code);
    bool VerifyTotp(Sys_User user,string code);
    Task SendEmailOtpAsync(Sys_User user,string otpKey);   // otpKey=pendingJti 或 "self:{userId}"；无邮箱抛 E-SEC-015，限流抛 E-SEC-016，发送失败抛 E-SEC-018
    Task<bool> VerifyEmailOtpAsync(string otpKey,string code);
    Task ResetAsync(Sys_User user,string? reason=null);
}
```

---

## Tasks 总览

| T | 范围 | 依赖 | 提交 |
|---|---|---|---|
| T1 | `Otp.NET` 包 + `TotpService`（密钥/URI/验码） | — | 1 |
| T2 | `TwoFactorOptions` + `Sys_User` 3 字段 + `Sys_Tenant.TwoFactorMode` + EF 迁移 | — | 1 |
| T3 | `IEmailSender`（Smtp + LogEmailSender 仅 Dev + 生产防护） | — | 1 |
| T4 | `IPendingTokenStore`（IDistributedCache 一次性）+ `TwoFactorService`（编排+密钥边界+邮件OTP绑key+邮件不绕过入会） | T1,T3 | 1 |
| T5 | `JwtHelper.GeneratePendingToken` + `AuthCookieWriter` cp6_2fa + WritePendingCookies(含 cp6_csrf) | — | 1 |
| T6 | login 2FA 分支 + `/auth/2fa/setup\|enroll\|verify\|email-otp`（pending 校验+一次性+CSRF+入会守卫）+ DI | T4,T5 | 1 |
| T7 | 自助 §4.6（status/setup-self/enroll-self/disable-self）+ admin reset(UserController) + 租户策略(TwoFactorPolicyController) + 权限点 seed | T4,T6 | 1 |
| T8 | E-SEC-011~019 五语 + sec.event.9~14 + 画面词条 seed | T6 | 1 |
| T9 | 前端：login 流 + 挑战/入会屏 + 自助/策略页 + api/types + qrcode | T6,T7,T8 | 1 |
| T10 | gstack 真浏览器 QA 全流程 | 全部 | — |

> 每 Task 先绿色构建+全量测试再本地 `git commit`（**不 push**，push 由用户监督）。subagent-driven：每 Task 派 subagent → spec审 + 质量审双过 → 先绿后 commit。基线现 995 测。

---

## Task T1：Otp.NET + TotpService

**Files:** Modify `CP6.Core/CP6.Core.csproj`；Create `CP6.Core/Services/Sys/ITotpService.cs`、`TotpService.cs`；Test `CP6.Tests/Sys/TotpServiceTests.cs`

- [ ] **Step 1: 加包** `CP6.Core.csproj` `<ItemGroup>` 加 `<PackageReference Include="Otp.NET" Version="1.4.0" />`；`dotnet restore CP6.Core`。
- [ ] **Step 2: 失败测试** `TotpServiceTests.cs`：
```csharp
using CP6.Core.Services.Sys;
using Microsoft.Extensions.Options;
using Xunit;
namespace CP6.Tests.Sys;
public class TotpServiceTests
{
    private static TotpService Make() => new(Options.Create(new SecurityOptions{ TwoFactor = new TwoFactorOptions{ Issuer="CP6", CodeWindow=1 } }));
    [Fact] public void Secret_generated_and_uri_contains_issuer_and_user()
    {
        var svc = Make(); var secret = svc.GenerateSecret();
        Assert.False(string.IsNullOrWhiteSpace(secret));
        var uri = svc.BuildOtpAuthUri(secret, "alice");
        Assert.StartsWith("otpauth://totp/", uri);
        Assert.Contains("issuer=CP6", uri);
        Assert.Contains("alice", uri);
    }
    [Fact] public void Verify_accepts_current_code_rejects_wrong()
    {
        var svc = Make(); var secret = svc.GenerateSecret();
        var now = OtpNet.Totp(OtpNet.Base32Encoding.ToBytes(secret)).ComputeTotp();
        Assert.True(svc.Verify(secret, now));
        Assert.False(svc.Verify(secret, "000000"));
        Assert.False(svc.Verify(secret, ""));
    }
}
```
（注：测试里 `OtpNet` 命名空间用法落码时核对 Otp.NET 实际 API：`new Totp(Base32Encoding.ToBytes(secret)).ComputeTotp()`。）
Run `dotnet test CP6.Tests --filter TotpServiceTests`，Expected 编译失败。
- [ ] **Step 3: 实现**（需 `TwoFactorOptions` 临时——本 Task 先在 `SecurityOptions.cs` 加 `TwoFactorOptions` 类与根属性 `public TwoFactorOptions TwoFactor{get;set;}=new();`，T2 不再重复）
`ITotpService.cs`：粘签名。`TotpService.cs`：
```csharp
using OtpNet;
using Microsoft.Extensions.Options;
namespace CP6.Core.Services.Sys;
public class TotpService : ITotpService
{
    private readonly TwoFactorOptions _o;
    public TotpService(IOptions<SecurityOptions> opt) => _o = opt.Value.TwoFactor;
    public string GenerateSecret() => Base32Encoding.ToString(KeyGeneration.GenerateRandomKey(20));
    public string BuildOtpAuthUri(string secret, string userName)
    {
        var iss = Uri.EscapeDataString(_o.Issuer); var u = Uri.EscapeDataString(userName);
        return $"otpauth://totp/{iss}:{u}?secret={secret}&issuer={iss}&period=30&digits=6";
    }
    public bool Verify(string secret, string code)
    {
        if (string.IsNullOrWhiteSpace(code)) return false;
        try { return new Totp(Base32Encoding.ToBytes(secret)).VerifyTotp(code, out _, new VerificationWindow(_o.CodeWindow, _o.CodeWindow)); }
        catch { return false; }
    }
}
```
Run filter，Expected PASS。
- [ ] **Step 4: 全量 + 提交** `dotnet build CP6.WebApi` + `dotnet test CP6.Tests`（全绿）。
```bash
git add -A && git commit -m "feat(sec): T1(2FA) Otp.NET + TotpService(密钥/otpauth URI/验码±窗口)
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T2：实体字段 + 配置 + 迁移

**Files:** Modify `CP6.Entity/DomainModels/Sys/Sys_User.cs`、`Sys/Sys_Tenant.cs`、`CP6.Core/Services/Sys/SecurityOptions.cs`（T1 已加 TwoFactorOptions，本 Task 校验齐全）；Create EF 迁移。

- [ ] **Step 1: Sys_User 加 3 字段**（`Email` 后）
```csharp
/// <summary>是否已启用 2FA（TOTP 绑定完成）</summary>
public bool TwoFactorEnabled { get; set; }
/// <summary>TOTP 密钥（base32）。MVP 明文存列（列加密见 spec §9）</summary>
[MaxLength(128)] public string? TwoFactorSecret { get; set; }
/// <summary>2FA 绑定时刻</summary>
public DateTime? TwoFactorEnrolledAt { get; set; }
```
- [ ] **Step 2: Sys_Tenant 加字段**
```csharp
/// <summary>租户 2FA 策略：0=关闭 1=可选 2=强制。默认 0。</summary>
public int TwoFactorMode { get; set; }
```
- [ ] **Step 3: 校验 TwoFactorOptions** 确认 T1 已在 `SecurityOptions.cs` 加 `TwoFactorOptions`（含 8 字段）+ 根 `TwoFactor` 属性；`appsettings.json` 的 `Security` 段加 `"TwoFactor": { "Issuer":"CP6","CodeWindow":1,"EmailOtpMinutes":5,"EmailOtpLength":6,"EmailResendCooldownSeconds":60,"PendingTokenMinutes":5,"EmailFallbackEnabled":true }`；加顶层 `"Email": { "Smtp": { "Host":"","Port":587,"User":"","Pass":"","From":"no-reply@cp6.local","EnableSsl":true } }`。
- [ ] **Step 4: 迁移** `dotnet ef migrations add TwoFactor --project CP6.Core --startup-project CP6.WebApi`，核对仅 `Sys_Users` 3 列 + `Sys_Tenants` 1 列 `AddColumn`，无新表。
- [ ] **Step 5: 全量 + 提交** `dotnet test CP6.Tests` 全绿。
```bash
git add -A && git commit -m "feat(sec): T2(2FA) Sys_User 2FA字段+Sys_Tenant.TwoFactorMode+TwoFactorOptions+迁移
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T3：IEmailSender（Smtp + LogEmailSender 仅 Dev + 生产防护）

**Files:** Create `CP6.Core/Services/Sys/IEmailSender.cs`、`SmtpEmailSender.cs`、`LogEmailSender.cs`；Test `CP6.Tests/Sys/EmailSenderTests.cs`

- [ ] **Step 1: 失败测试**（LogEmailSender 记日志不抛）
```csharp
using CP6.Core.Services.Sys;
using Microsoft.Extensions.Logging.Abstractions;
using Xunit;
namespace CP6.Tests.Sys;
public class EmailSenderTests
{
    [Fact] public async Task Log_sender_does_not_throw()
    { var s = new LogEmailSender(NullLogger<LogEmailSender>.Instance); await s.SendAsync("a@b.c","sub","body with 123456"); }
}
```
- [ ] **Step 2: 实现** `IEmailSender.cs`（签名）；`LogEmailSender.cs`：
```csharp
using Microsoft.Extensions.Logging;
namespace CP6.Core.Services.Sys;
public class LogEmailSender : IEmailSender
{
    private readonly ILogger<LogEmailSender> _log;
    public LogEmailSender(ILogger<LogEmailSender> log) => _log = log;
    public Task SendAsync(string to, string subject, string body)
    { _log.LogWarning("[DEV-EMAIL→{To}] {Subject}: {Body}", to, subject, body); return Task.CompletedTask; }
}
```
`SmtpEmailSender.cs`：读 `IConfiguration` 的 `Email:Smtp`，`System.Net.Mail.SmtpClient` 发送（`MailMessage(from,to){Subject,Body}`，`new SmtpClient(host,port){Credentials,EnableSsl}`，`SendMailAsync`；异常向上抛由调用方转 E-SEC-018）。
Run filter PASS。
- [ ] **Step 3: DI 工厂 + 生产防护**（Program.cs，T6 统一注册区亦可，但本 Task 落工厂逻辑）：
```csharp
builder.Services.AddScoped<CP6.Core.Services.Sys.IEmailSender>(sp => {
    var cfg = sp.GetRequiredService<IConfiguration>();
    var env = sp.GetRequiredService<IWebHostEnvironment>();
    var host = cfg["Email:Smtp:Host"];
    if (!string.IsNullOrWhiteSpace(host)) return new CP6.Core.Services.Sys.SmtpEmailSender(cfg);
    if (env.IsDevelopment()) return new CP6.Core.Services.Sys.LogEmailSender(sp.GetRequiredService<ILogger<CP6.Core.Services.Sys.LogEmailSender>>());
    var twoFa = sp.GetRequiredService<IOptions<CP6.Core.Services.Sys.SecurityOptions>>().Value.TwoFactor;
    if (twoFa.EmailFallbackEnabled) throw new InvalidOperationException("生产环境启用邮件OTP回退但未配置 Email:Smtp:Host——拒绝以日志兜底(防OTP泄露)；配置SMTP或设 Security:TwoFactor:EmailFallbackEnabled=false");
    return new CP6.Core.Services.Sys.LogEmailSender(sp.GetRequiredService<ILogger<CP6.Core.Services.Sys.LogEmailSender>>()); // 仅当显式关闭邮件回退(不会被调用)
});
```
- [ ] **Step 4: 全量 + 提交**
```bash
git add -A && git commit -m "feat(sec): T3(2FA) IEmailSender(Smtp+LogEmailSender仅Dev)+生产无SMTP启动防护
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T4：PendingTokenStore + TwoFactorService

**Files:** Create `CP6.Core/Services/Sys/IPendingTokenStore.cs`、`PendingTokenStore.cs`、`ITwoFactorService.cs`、`TwoFactorService.cs`；Test `CP6.Tests/Sys/PendingTokenStoreTests.cs`、`TwoFactorServiceTests.cs`

- [ ] **Step 1: PendingTokenStore 失败测试 + 实现**（IDistributedCache 一次性，评审#2）
`PendingTokenStoreTests.cs`：
```csharp
using CP6.Core.Services.Sys;
using Microsoft.Extensions.Caching.Distributed;
using Microsoft.Extensions.Caching.Memory;
using Microsoft.Extensions.Options;
using Xunit;
namespace CP6.Tests.Sys;
public class PendingTokenStoreTests
{
    private static PendingTokenStore Make(){ IDistributedCache c=new MemoryDistributedCache(Options.Create(new MemoryDistributedCacheOptions())); return new(c, Options.Create(new SecurityOptions())); }
    [Fact] public void Create_get_consume_is_one_time()
    {
        var s=Make(); var uid=Guid.NewGuid(); var tid=Guid.NewGuid();
        var jti=s.Create(uid,tid,"2fa_verify");
        var got=s.Get(jti); Assert.NotNull(got); Assert.Equal(uid,got!.Value.userId); Assert.Equal("2fa_verify",got.Value.purpose);
        s.Consume(jti); Assert.Null(s.Get(jti));   // 一次性：消费后不存在
    }
}
```
`PendingTokenStore.cs`：key `sec:2fa:pending:{jti}`，value JSON `{userId,tenantId,purpose}`，`AbsoluteExpirationRelativeToNow=PendingTokenMinutes`；`Create` 生成 `Guid.NewGuid().ToString("N")` 作 jti；`Consume`=`Remove`。
Run filter PASS。
- [ ] **Step 2: TwoFactorService 失败测试**（核心矩阵 + 评审#4/#7）
`TwoFactorServiceTests.cs`（用 InMemory + MemoryDistributedCache + TotpService + 假 IEmailSender）：
```csharp
[Fact] public void MustEnroll_only_when_required_and_not_enabled() { /* tenantMode 0/1/2 × enabled t/f 矩阵 */ }
[Fact] public async Task ConfirmEnrollment_sets_enabled_with_valid_totp() { /* BeginEnrollment→算当前码→ConfirmEnrollmentAsync→TwoFactorEnabled=true,EnrolledAt!=null */ }
[Fact] public void BeginEnrollment_rejects_when_already_enabled() { /* user.TwoFactorEnabled=true→Assert.Throws<InvalidOperationException>(()=>svc.BeginEnrollment(user)) 含 "E-SEC-017" */ }   // 评审#7
[Fact] public async Task Email_otp_store_verify_one_time_and_rate_limit() { /* SendEmailOtpAsync 存→VerifyEmailOtpAsync 真码 true 再验 false(已消费);重发命中 cooldown 抛 E-SEC-016;无邮箱抛 E-SEC-015 */ }
[Fact] public async Task Reset_clears_secret_and_enabled() { /* ResetAsync→Enabled=false,Secret=null,EnrolledAt=null */ }
```
Run filter 编译失败。
- [ ] **Step 3: TwoFactorService 实现** 按 §3.4 签名。要点：
  - `ResolveTenantMode`：`_db.Sys_Tenants.IgnoreQueryFilters().Where(t=>t.Id==tenantId).Select(t=>t.TwoFactorMode).FirstOrDefault()`。
  - `IsChallengeRequired`=`user.TwoFactorEnabled || mode==2`；`MustEnroll`=`mode==2 && !user.TwoFactorEnabled`。
  - `BeginEnrollment`：若 `user.TwoFactorEnabled` → `throw new InvalidOperationException("E-SEC-017")`；否则 `user.TwoFactorSecret=_totp.GenerateSecret()`（**不**置 Enabled，调用方 SaveChanges）；返 `_totp.BuildOtpAuthUri(secret,user.UserName)`。
  - `ConfirmEnrollmentAsync`：`_totp.Verify(user.TwoFactorSecret,code)` → 真则 `TwoFactorEnabled=true;TwoFactorEnrolledAt=DateTime.Now`，审计 Enrolled，返 true；假返 false。
  - `VerifyTotp`=`_totp.Verify(user.TwoFactorSecret,code)`。
  - `SendEmailOtpAsync(user,otpKey)`：`user.Email` 空→`throw E-SEC-015`；cooldown key 命中→`throw E-SEC-016`；生成 N 位数字 OTP（`RandomNumberGenerator`），存 `sec:2fa:otp:{otpKey}`=SHA256(otp) TTL=EmailOtpMinutes + 设 cooldown key；`try await _email.SendAsync(user.Email,"验证码",otp) catch → throw E-SEC-018`；审计 EmailOtpSent。
  - `VerifyEmailOtpAsync(otpKey,code)`：读 `sec:2fa:otp:{otpKey}` 比对 SHA256，真则 Remove 返 true。
  - `ResetAsync`：`Enabled=false;Secret=null;EnrolledAt=null`，审计 Reset。
  - **密钥/审计边界（评审#7）**：审计 LogAsync 的 reason **不传** secret/uri。
Run filter PASS。
- [ ] **Step 4: 全量 + 提交**
```bash
git add -A && git commit -m "feat(sec): T4(2FA) PendingTokenStore(IDistributedCache一次性)+TwoFactorService(编排/密钥边界E-SEC-017/邮件OTP绑key/无邮箱E-SEC-015/限流016/发送失败018)
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T5：pending 令牌 JWT + Cookie

**Files:** Modify `CP6.Core/Utilities/JwtHelper.cs`、`CP6.Core/Services/Sys/AuthCookieWriter.cs`、`IAuthCookieWriter.cs`；Test `CP6.Tests/Sys/PendingCookieTests.cs`（或并入既有）

- [ ] **Step 1: JwtHelper.GeneratePendingToken**
```csharp
public static string GeneratePendingToken(string userId, Guid tenantId, string purpose, string pendingJti, string secret, string issuer, string audience, int minutes)
{
    var claims = new[] {
        new Claim(ClaimTypes.NameIdentifier, userId),
        new Claim("tenant_id", tenantId.ToString()),
        new Claim("token_use", purpose),                 // 2fa_verify | 2fa_enroll
        new Claim(JwtRegisteredClaimNames.Jti, pendingJti)
    };
    var key = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(secret));
    var token = new JwtSecurityToken(issuer, audience, claims, expires: DateTime.Now.AddMinutes(minutes),
        signingCredentials: new SigningCredentials(key, SecurityAlgorithms.HmacSha256));
    return new JwtSecurityTokenHandler().WriteToken(token);
}
```
- [ ] **Step 2: AuthCookieWriter cp6_2fa**（接口加 `WritePendingCookies(HttpResponse,string pendingToken,string csrf)` / `ClearPendingCookies(HttpResponse)`）
```csharp
public const string PendingCookie = "cp6_2fa";
public void WritePendingCookies(HttpResponse resp, string pendingToken, string csrf)
{
    resp.Cookies.Append(PendingCookie, pendingToken, new CookieOptions{ HttpOnly=true, Secure=_c.Secure, SameSite=Same, Path=RefreshPath });
    resp.Cookies.Append(CsrfCookie, csrf, new CookieOptions{ HttpOnly=false, Secure=_c.Secure, SameSite=Same, Path="/" });   // 评审#1
}
public void ClearPendingCookies(HttpResponse resp)
{ resp.Cookies.Append(PendingCookie,"",new CookieOptions{ Expires=DateTimeOffset.UnixEpoch, Path=RefreshPath, Secure=_c.Secure, SameSite=Same }); }
```
`ClearAuthCookies` 的清单加 `(PendingCookie, RefreshPath)`（登出/改密一并清 cp6_2fa）。
- [ ] **Step 3: 测试** `PendingCookieTests.cs`：`GeneratePendingToken` 产出可被 `JwtSecurityTokenHandler` 读出 jti/token_use/tenant_id；`WritePendingCookies` 后 `resp` 含 cp6_2fa(httpOnly) + cp6_csrf(非 httpOnly)（用 `DefaultHttpContext().Response` 断言 `Headers["Set-Cookie"]`）。Run PASS。
- [ ] **Step 4: 全量 + 提交**
```bash
git add -A && git commit -m "feat(sec): T5(2FA) JwtHelper.GeneratePendingToken(jti/purpose)+AuthCookieWriter cp6_2fa+WritePendingCookies(含cp6_csrf评审#1)
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T6：登录 2FA 分支 + /auth/2fa/* 端点 + DI

**Files:** Modify `AuthController.cs`、`Program.cs`（DI：ITotpService/IEmailSender 工厂/IPendingTokenStore/ITwoFactorService）；Test `CP6.Tests/Sys/AuthController2faTests.cs`

- [ ] **Step 1: DI 注册**（Program.cs，2FA 服务区）：`AddScoped<ITotpService,TotpService>()` + `AddScoped<IPendingTokenStore,PendingTokenStore>()` + `AddScoped<ITwoFactorService,TwoFactorService>()` + IEmailSender 工厂（T3）。AuthController 构造注入 `ITwoFactorService _2fa`、`IPendingTokenStore _pending`。
- [ ] **Step 2: login 分支**（§4.1 伪码落码）。`_tenant.CurrentTenantId=user.TenantId` 后插：
```csharp
var mode = _2fa.ResolveTenantMode(user.TenantId);
if (_2fa.IsChallengeRequired(user, mode))
{
    var purpose = _2fa.MustEnroll(user, mode) ? "2fa_enroll" : "2fa_verify";
    var pendingJti = _pending.Create(user.Id, user.TenantId, purpose);
    var jwt = _config.GetSection("JWT");
    var pendingToken = JwtHelper.GeneratePendingToken(user.Id.ToString(), user.TenantId, purpose, pendingJti, jwt["Secret"]!, jwt["Issuer"]!, jwt["Audience"]!, _sec.TwoFactor.PendingTokenMinutes);
    _cookies.WritePendingCookies(Response, pendingToken, AuthCookieWriter.NewCsrfToken());
    await _audit.LogAsync(SecurityEventType.TwoFactorChallenged, user.Id, user.UserName, request.TenantCode, ClientIp, ClientUa, purpose == "2fa_enroll" ? "enroll" : null);
    return Ok(new { twoFactorRequired = true, mustEnroll = purpose == "2fa_enroll" });
}
// 否则照旧 RecordSuccess+审计+IssueAsync+WriteAuthCookies+返档案
```
- [ ] **Step 3: pending 读取助手 + 4 端点**。私有 `(Sys_User user,string jti,string purpose) ReadPending(string expectedPurpose=null)`：读 cp6_2fa cookie → 校验 JWT（签名/iss/aud/exp）→ 取 jti → `_pending.Get(jti)`（null→`BizException("E-SEC-013")`）→ 可选校验 purpose → 加载 user（IgnoreQueryFilters）+ 回设 `_tenant.CurrentTenantId`。端点（均 `[AllowAnonymous]`，CSRF 由 CsrfMiddleware 强制，非 login 豁免=自动校验 X-CSRF-Token）：
  - `POST /auth/2fa/setup`：ReadPending → `BeginEnrollment`（catch InvalidOperationException 转 BizException E-SEC-017）→ SaveChanges → 返 `{otpauthUri, secret}`。
  - `POST /auth/2fa/enroll` body `{code}`：ReadPending("2fa_enroll") → `ConfirmEnrollmentAsync` → 真则 `_pending.Consume(jti)` + 登录完成（RecordSuccess+审计 Enrolled+LoginSuccess+IssueAsync+WriteAuthCookies+ClearPendingCookies）返档案；假返 E-SEC-011。
  - `POST /auth/2fa/verify` body `{code,method}`：ReadPending("2fa_verify") → `EnsureNotLocked`（catch→E-SEC-002）→ method=="email"?`VerifyEmailOtpAsync(jti,code)`:`VerifyTotp` → 真则 `_pending.Consume(jti)`+登录完成（审计 TwoFactorVerified+LoginSuccess）返档案；假则 `RecordFailureAsync`+审计 TwoFactorFailed+E-SEC-011。
  - `POST /auth/2fa/email-otp`：ReadPending → **purpose=="2fa_enroll"→BizException("E-SEC-014")（评审#4）**；否则 `SendEmailOtpAsync(user,jti)`（catch InvalidOperationException 转 BizException：E-SEC-015/016/018）返 `{code:0}`。
- [ ] **Step 4: 失败测试 + 实现** `AuthController2faTests.cs`（直 new 控制器 + InMemory + MemoryDistributedCache，仿 AuthControllerChangePasswordTests）：①租户 mode=2 未启用 login → 返 twoFactorRequired+mustEnroll，**不写三 Cookie**；②已启用 login → twoFactorRequired，mustEnroll=false；③verify 正确 TOTP → 写三 Cookie + pending 被消费（再 verify 同 jti → E-SEC-013）；④verify 错码 → E-SEC-011 + FailedLoginCount++；⑤enroll 态 email-otp → E-SEC-014。Run PASS。
- [ ] **Step 5: 全量 + 提交**
```bash
git add -A && git commit -m "feat(sec): T6(2FA) 登录2FA分支(pending+缓存状态+cp6_csrf)+/auth/2fa/setup·enroll·verify·email-otp(一次性消费+CSRF强制+入会态禁邮件E-SEC-014)+DI
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T7：自助启停 + admin 重置 + 租户策略 + 权限点

**Files:** Modify `AuthController.cs`（§4.6 自助）、`UserController.cs`（reset-2fa）、`Program.cs`（权限点 seed）；Create `Controllers/Sys/TwoFactorPolicyController.cs`；Test `CP6.Tests/Sys/TwoFactorSelfServiceTests.cs`

- [ ] **Step 1: 自助 §4.6**（AuthController，全 `[Authorize]`，当前用户 `(await _perm.GetAsync()).UserId`）：
  - `GET /api/auth/2fa/status` → `{enabled, tenantMode, canDisable=enabled && tenantMode!=2}`。
  - `POST /api/auth/2fa/setup-self` → 已 Enabled 拒 E-SEC-017；否则 BeginEnrollment+SaveChanges 返 `{otpauthUri,secret}`。
  - `POST /api/auth/2fa/enroll-self` body `{code}` → ConfirmEnrollmentAsync 返 `{code:0}`。
  - `POST /api/auth/2fa/disable-self` body `{currentPassword,code,method}` → `_hasher.Verify` 失败 E-SEC-006；租户 mode==2 → E-SEC-019；method 验码（TOTP 或 `VerifyEmailOtpAsync("self:{uid}",code)`，自助邮件用 `SendEmailOtpAsync(user,"self:{uid}")`）失败 E-SEC-011；全过 ResetAsync(reason="self-disable")+SaveChanges。
  （自助邮件发送端点 `POST /api/auth/2fa/email-otp-self` [Authorize] → `SendEmailOtpAsync(user,"self:{uid}")`。）
- [ ] **Step 2: admin reset**（UserController 加，`[RequirePermission("user","edit")]`）：
```csharp
[HttpPost("reset-2fa")][RequirePermission("user","edit")]
public async Task<IActionResult> ResetTwoFactor([FromBody] Guid userId)
{ var u=await _context.Sys_Users.FirstOrDefaultAsync(x=>x.Id==userId); if(u==null) return NotFound();
  await _2fa.ResetAsync(u,"admin-reset"); await _context.SaveChangesAsync(); return Ok(new{code=0}); }
```
（UserController 构造注入 `ITwoFactorService _2fa`。）
- [ ] **Step 3: 租户策略控制器** `TwoFactorPolicyController.cs`（`[Authorize]`，作用当前租户 `_tenant.CurrentTenantId`）：
  - `GET /api/sys/two-factor-policy` → `{mode}`。
  - `PUT /api/sys/two-factor-policy` body `{mode}` `[RequirePermission("user","edit")]` → 0/1/2 校验 → 写 `Sys_Tenant.TwoFactorMode`（`IgnoreQueryFilters` 取当前租户行）+ SaveChanges。
- [ ] **Step 4: 权限点 seed**（Program.cs，授权加固 seed 块同区）：菜单 104(user) 已有 query/add/edit/delete（授权加固已 seed），reset-2fa/策略复用 `user:edit`，**无需新增**权限点。确认即可。
- [ ] **Step 5: 失败测试 + 实现** `TwoFactorSelfServiceTests.cs`：①disable-self 缺密码/错码 → 拒；②租户 mode=2 disable-self → E-SEC-019；③admin reset 清 Enabled；④setup-self 已启用 → E-SEC-017。Run PASS。
- [ ] **Step 6: 全量 + 提交**
```bash
git add -A && git commit -m "feat(sec): T7(2FA) 自助启停(disable-self验密码+码,租户强制禁关E-SEC-019)+admin重置+租户策略控制器
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T8：i18n 五语

**Files:** Create `CP6.WebApi/Seed/I18nSec2faScreenSeed.cs`；Modify `Program.cs`（i18n `.Concat`）

- [ ] **Step 1: seed**（仿 `I18nSecScreenSeed`，`.Items` Sys_Lang[]）：
  - E-SEC-011~019 九码五语（文案见 spec §5 表，ZhCN/ZhTW/En/Ja/Ko）。
  - `sec.event.9~14` 六枚举标签（双因素挑战/验证/失败/绑定/重置/邮件验证码已发，五语）。
  - 画面词条：`sec.2fa.{title,scanQr,enterCode,useEmail,sendEmailCode,emailSent,enable,disable,status.on,status.off,enrollTitle,policyTitle,policy.off,policy.optional,policy.required,confirmDisable}` 等，五语。
- [ ] **Step 2: 接链** Program.cs i18n `.Concat(CP6.WebApi.Seed.I18nSec2faScreenSeed.Items)`。
- [ ] **Step 3: 构建 + 提交** `dotnet build CP6.WebApi` + `dotnet test CP6.Tests` 全绿。
```bash
git add -A && git commit -m "feat(sec): T8(2FA) E-SEC-011~019+sec.event.9~14+2FA画面词条 五语seed
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T9：前端

**Files:** Modify `cp6.web/package.json`（加 `qrcode`）、`src/views/LoginView.vue`、`src/router/index.ts`；Create `src/views/pms/TwoFactorChallengeView.vue`、`TwoFactorEnrollView.vue`、`src/views/pms/TwoFactorSettingsView.vue`（自助+策略，或并入个人设置/租户设置现有页）、`src/api/sys/twoFactor.ts`、`src/types/sys/twoFactor.ts`

- [ ] **Step 1: 包 + api + types** `npm i qrcode`（+ `@types/qrcode` dev）。`api/sys/twoFactor.ts`：`twoFactorApi = { setup/enroll/verify/emailOtp(登录期), status/setupSelf/enrollSelf/disableSelf/emailOtpSelf, getPolicy/setPolicy }`（对应端点）。`types/sys/twoFactor.ts`：`TwoFactorMode` 枚举(0/1/2)。
- [ ] **Step 2: 登录流**（LoginView）：`authApi.login` 返 `twoFactorRequired` → **不置 cp6_authed**；`mustEnroll` → `router.push('/sys/2fa-enroll')`，否则 `/sys/2fa-challenge`。
- [ ] **Step 3: 挑战/入会屏**（standalone 静态路由，仿 ChangePasswordView）：
  - `TwoFactorChallengeView.vue`：TOTP 码输入 + "改用邮件"按钮（调 emailOtp 发码，method 切 email）→ `twoFactorApi.verify({code,method})` 成功置 cp6_authed + 跳首页。
  - `TwoFactorEnrollView.vue`：调 setup 拿 otpauthUri → `qrcode` 渲染二维码 + 手输 secret 备选 → 输码 `twoFactorApi.enroll({code})` 成功置 cp6_authed + 跳首页。
  - router：`staticRoutes` 加 `/sys/2fa-challenge`、`/sys/2fa-enroll`（`meta:{standalone:true}`）。
- [ ] **Step 4: 自助 + 策略**：个人设置页加 2FA 区（status → 启用走 setup-self+enroll-self 扫码；停用走 disable-self 验密码+码，租户 Required 时禁用按钮）；租户设置/系统页加 2FA 策略下拉（getPolicy/setPolicy）。
- [ ] **Step 5: 前端校验** `cd cp6.web && npm run type-check && npx vitest run && npm run build`（全绿；i18n:check 留 T10 起后端 i18n:pull 后）。
```bash
git add -A && git commit -m "feat(sec): T9(2FA) 前端登录2FA流+挑战/入会屏(qrcode)+自助启停+租户策略+api/types
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task T10：gstack 真浏览器 QA

> 本地 5177 后端 / 5173 前端。新词须真起后端 + `npm run i18n:pull` 重建快照 + `i18n:gen-types` + `i18n:check` 绿。测试需 2~3 个租户（mode=0/1/2）+ 测试用户。

- [ ] **Step 1: i18n 重建** 起后端 → `cd cp6.web && npm run i18n:pull` + `i18n:gen-types` + `i18n:check` 绿。
- [ ] **Step 2: 可选模式自助绑** 设某租户 mode=1，用户个人设置启用 2FA（扫码/手输 secret 到认证器→输码绑定）；再登出重登 → 挑战屏输 TOTP 过。
- [ ] **Step 3: 强制租户首登强制入会** 设租户 mode=2，新用户登录 → mustEnroll → 入会屏绑定 → 进系统；入会态点"改用邮件" → 被拒(E-SEC-014，评审#4)。
- [ ] **Step 4: 挑战 TOTP 过/错** 错码 → E-SEC-011 + 计锁定（连错达阈值锁定 E-SEC-002）。
- [ ] **Step 5: 邮件回退** 已启用用户挑战屏点"改用邮件" → dev 日志取 OTP → 验过；重发太快 → E-SEC-016。
- [ ] **Step 6: 自助停 + admin 重置** 可选模式用户 disable-self（验密码+码）→ 关；强制租户 disable-self → E-SEC-019 拒；admin 在用户管理 reset-2fa 某用户 → 该用户重新入会。
- [ ] **Step 7: 安全边界** ①pending 态打受保护接口 → 401；②2FA POST 去掉 X-CSRF-Token（开 CSRF）→ 403（评审#1）；③重放已用 pending → E-SEC-013（评审#2）；④确认响应/SecurityLog 不含 TwoFactorSecret/otpauthUri（评审#7）；⑤cp6_authed 仅 2FA 后置。
- [ ] **Step 8: 全量回归** `dotnet test CP6.Tests` + 前端四校验全绿。
```bash
git add -A && git commit -m "test(sec): T10(2FA) gstack真浏览器QA全流程(入会/挑战/邮件回退/自助停/admin重置/CSRF/pending一次性)
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review（计划自审）

**1. Spec 覆盖**：spec §2 数据模型→T2；§3 服务→T1(Totp)/T3(Email)/T4(Pending+2FA Service)/T5(令牌Cookie)；§4 流程→登录T6/入会T6/挑战T6/自助§4.6 T7/admin重置T7/租户策略T7；§5 错误码→T8；§6 配置→T2;§7 i18n/前端→T8/T9;§8 测试→各Task+T10;§9 hardening=不做(记录)。**无遗漏**。
**2. 评审 8 点映射**：#1 CSRF→T5(WritePendingCookies含csrf)+T6(端点不豁免);#2 pending一次性→T4(PendingTokenStore)+T6(Consume+E-SEC-013);#3 OTP绑pendingJti→T4(otpKey=jti)+T6;#4 邮件不绕过入会→T6(email-otp purpose守卫E-SEC-014);#5 自助API→T7;#6 LogEmailSender仅Dev+生产防护→T3;#7 密钥边界→T4(BeginEnrollment拒E-SEC-017+审计不带secret)+T7(setup-self);#8 错误码→T8(011~019)。**全覆盖**。
**3. 类型一致**：`ITwoFactorService`/`IPendingTokenStore`/`ITotpService`/`IEmailSender` 签名跨 T1/T3/T4/T6/T7 一致；otpKey 语义（pendingJti 或 self:{uid}）跨 T4/T6/T7 一致；`TwoFactorOptions` 8 字段 T1 定义后各 Task 复用。
**待落码前确认**（非阻塞，首 Task grep 核定）：①Otp.NET 实际 API（`Totp`/`Base32Encoding`/`VerificationWindow` 命名空间 `OtpNet`）②`IWebHostEnvironment.IsDevelopment()` 在工厂 sp 可解析 ③`appsettings.Development.json` 是否需 `Email:Smtp:Host` 留空（是，走 LogEmailSender）。

---

*生成于 2026-06-22。源 spec：`docs/superpowers/specs/2026-06-22-2fa-design.md`（c222b4a）。执行：subagent-driven，每 Task spec审+质量审双过 + 先绿后本地 commit（不 push，用户监督）+ gstack QA。基线 995 测。*
