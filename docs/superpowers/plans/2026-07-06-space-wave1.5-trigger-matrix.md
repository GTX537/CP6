# Space 波1.5：发布触发矩阵兑现（H2 缩格护栏 / H3 删除护栏+mode / H4 改挂 re-publish）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 兑现契约 04 第 4/7 章「编辑动作 ↔ 发布联动」的另一半——缩格护栏（H2）、删巷道/货架发布护栏与两条放行路径 mode=deactivate|rehome（H3 + 契约 §7.2 完整实现，2026-07-06 用户拍板）、货架改挂库区/巷道自动 re-publish（H4）、库位删除通道（草稿+停用可删，已发布拒绝）。

**Architecture:** 基线 main=4e0e7bf（波1 已并入）。核心新基元 = `ILocationPublishService.RepublishAsync`（§7.2 路径B：码冻结不变、Version+1、UPSERT 事件刷新 WMS path 元数据），被 SceneService（改挂检测）与 SpaceMasterService（rehome 模式）复用；`SpaceMasterService` 新增 `ILocationPublishService` 依赖（无循环：LocationPublishService 不依赖主数据服务）。删除护栏哲学 = 默认 Restrict、mode 显式放行，与波1 停用决策模型同构。

**Tech Stack:** .NET 8 + EF Core（同波1）。无新表无迁移。

## Global Constraints

- 契约硬约束：`docs/space/04-publish-contract.md` §4（发布触发矩阵）/ §7.1（默认 Restrict：E-SPACE-402 巷道 / E-SPACE-403 货架）/ §7.2（路径 A=先停用再删、路径 B=改挂 re-publish：**LocationCode 全程不变，只刷 path、Version+1**）。
- **2026-07-06 用户拍板（对契约字面的三项裁决，计划内有效）**：① 缩格触及已发布库位 → **Restrict 阻断**（非契约 §4 字面的自动 DEACTIVATE），草稿/停用越界连带删；② 库位删除通道对 **Status=0 草稿与 Status=2 停用**开放，Status=1 拒绝（已知代价：停用位删除后其码仍被 T_WmsBin 停用行占据 join 锚，同码新发布会被锚碰撞 REJECTED——清理机制记后续票）；③ **完整实现 ?mode=rehome**（巷道 + 货架都做）。
- 错误码：新码使用前 **grep 全仓确认未被占用**（`E-SPACE-406` 等；若与总纲 Spec `docs/superpowers/specs/CP6_Space3D_*.md:1131-1177` 段语义冲突则顺延取号并在报告说明）。消息格式沿用 `InvalidOperationException("E-SPACE-xxx: 中文描述")`。
- 事务惯例：`IDbContextTransaction? tx = _db.Database.IsRelational() ? await _db.Database.BeginTransactionAsync() : null;`；**嵌套守卫**：可能被外层事务包裹的方法须加 `_db.Database.CurrentTransaction == null` 检查（同连接嵌套 BeginTransaction 会抛）。
- mode=deactivate 的停用循环**不包事务**（停用是同步 RPC 决策模型，部分完成=已停用的保持停用，重试幂等收敛——注释说明即可）。
- 测试基线：`dotnet test CP6.Tests/CP6.Tests.csproj` = **1532 passed / 5 skipped**。构建 `dotnet build CP6.slnx`（不是 .sln）。
- InMemory 单测惯例照波1（`Db()` 帮手 / SceneServiceTests 的 `Make()`）。
- 多租户铁律：查询不写 `.Where(TenantId)`；创建实体不写 TenantId。
- 提交 `feat(space):` / `fix(space):` + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`，每 Task 一个 commit。
- **既有测试语义变更预告**：SceneServiceTests 与 SpaceMasterServiceTests 中「删有库位货架 → E-SPACE-003」的既有测试固化的是旧全拦语义，Task 4/6 落地后须改为新语义断言（草稿级联删 / 已发布 E-SPACE-403），commit message 说明。

---

### Task 1: RepublishAsync 基元（§7.2 路径B 的发布侧原语）

**Files:**
- Modify: `CP6.Core/Services/Space/ILocationPublishService.cs`（加 1 方法）
- Modify: `CP6.Core/Services/Space/LocationPublishService.cs`（加实现，`DeactivateAsync` 之后）
- Test: `CP6.Tests/LocationPublishServiceTests.cs`（新增 3 个测试）

**Interfaces:**
- Consumes: 波1 的 `BuildItemAsync`（私有，含 WarehouseCd 映射与长度守卫）、`_hook.OnLocationPublishedAsync`、`DocNumber.NextAsync`
- Produces: `Task<int> RepublishAsync(IReadOnlyCollection<Guid> locationIds, string? user)`——只处理 Status=1 且有码的库位；Version+1；发 UPSERT 事件；返回条数。**带嵌套事务守卫**（Task 2/5/6 会在外层事务/无事务两种环境下调用）。

- [ ] **Step 1: 写失败测试**

`CP6.Tests/LocationPublishServiceTests.cs`，加在 `// ── D-5: 采纳导入 ──` 注释之前：

```csharp
    // ── §7.2 路径B: re-publish ────────────────────────────────────────────

    [Fact]
    public async Task Republish_PublishedLocation_BumpsVersion_EmitsUpsert()
    {
        using var db = Db();
        var (floorId, rackId) = SeedHierarchy(db, siteWarehouseCd: null);
        var locId = Guid.NewGuid();
        db.Space_Locations.Add(new Space_Location
        {
            Id = locId, FloorId = floorId, RackId = rackId,
            Placed = true, Status = 1, CodeOrigin = 1, LocationCode = "A-01-01-01",
            Col = 1, Level = 1, Depth = 1, Version = 3
        });
        await db.SaveChangesAsync();

        var n = await MakePublishSvc(db).RepublishAsync(new[] { locId }, "u");

        Assert.Equal(1, n);
        var loc = await db.Space_Locations.SingleAsync();
        Assert.Equal(1, loc.Status);                    // 状态不变（不是重新发布生命周期）
        Assert.Equal(4, loc.Version);                   // 只升版
        Assert.Equal("A-01-01-01", loc.LocationCode);   // 码冻结不变（§7.2 B 精髓）
        var evt = await db.IntegrationEvents.SingleAsync();
        var payload = System.Text.Json.JsonSerializer.Deserialize<System.Text.Json.JsonElement>(evt.PayloadJson);
        Assert.Equal("UPSERT", payload.GetProperty("Items")[0].GetProperty("Op").GetString());
        Assert.Equal(4, payload.GetProperty("Items")[0].GetProperty("Version").GetInt64());
    }

    [Fact]
    public async Task Republish_IgnoresDraftAndDeactivated()
    {
        using var db = Db();
        var draftId = Guid.NewGuid();
        var deactId = Guid.NewGuid();
        db.Space_Locations.Add(new Space_Location
        {
            Id = draftId, FloorId = Guid.NewGuid(), Status = 0, LocationCode = "D-01", Version = 0
        });
        db.Space_Locations.Add(new Space_Location
        {
            Id = deactId, FloorId = Guid.NewGuid(), Status = 2, LocationCode = "X-01", Version = 2
        });
        await db.SaveChangesAsync();

        var n = await MakePublishSvc(db).RepublishAsync(new[] { draftId, deactId }, "u");

        Assert.Equal(0, n);
        Assert.Equal(0, await db.IntegrationEvents.CountAsync());   // 非发布态不产生事件
        Assert.Equal(0, (await db.Space_Locations.FirstAsync(l => l.Id == draftId)).Version);
    }

    [Fact]
    public async Task Republish_EmptyInput_Returns0_NoEvent()
    {
        using var db = Db();
        var n = await MakePublishSvc(db).RepublishAsync(Array.Empty<Guid>(), "u");
        Assert.Equal(0, n);
        Assert.Equal(0, await db.IntegrationEvents.CountAsync());
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~Republish"`
Expected: FAIL——编译错（接口无 `RepublishAsync`）。

- [ ] **Step 3: 接口 + 实现**

① `ILocationPublishService.cs` 在 `DeactivateAsync` 声明之后加：

```csharp
    /// <summary>
    /// 路径元数据 re-publish（ch04 §7.2 路径B）：层级归属（Zone/Aisle/Rack 挂载）变更后，
    /// 对已发布(Status=1)库位 Version+1 并发 UPSERT 事件刷新 WMS 侧 path——码冻结不变、状态不变。
    /// 非发布态/无码的 id 自动忽略。返回实际 re-publish 条数。
    /// </summary>
    Task<int> RepublishAsync(IReadOnlyCollection<Guid> locationIds, string? user);
```

② `LocationPublishService.cs` 在 `DeactivateAsync` 方法之后加：

```csharp
    /// <inheritdoc/>
    public async Task<int> RepublishAsync(IReadOnlyCollection<Guid> locationIds, string? user)
    {
        if (locationIds.Count == 0) return 0;

        // 嵌套事务守卫：本方法会被 SceneService 场景保存事务（H4 改挂）或删除放行路径包裹调用；
        // 已有环境事务时直接加入（同连接嵌套 BeginTransaction 会抛），无事务时自开（惯例守卫）。
        var ownsTx = _db.Database.IsRelational() && _db.Database.CurrentTransaction == null;
        IDbContextTransaction? tx = ownsTx ? await _db.Database.BeginTransactionAsync() : null;
        try
        {
            var locs = await _db.Space_Locations
                .Where(l => locationIds.Contains(l.Id) && l.Status == 1 && l.LocationCode != null)
                .ToListAsync();
            if (locs.Count == 0) return 0;

            var (_, seq) = await DocNumber.NextAsync(_db, "LPB");
            var batch = new LocationPublishBatch
            {
                BatchNo = $"LPUB-{DateTime.Today:yyyyMMdd}-{seq:D4}",
                TenantId = _t.CurrentTenantId,
                PublishedBy = user
            };
            foreach (var l in locs)
            {
                l.Version += 1;                 // 码冻结不变，只升版刷新 path（§7.2 B）
                l.Modifier = user;
                l.ModifyDate = DateTime.Now;
                batch.Items.Add(await BuildItemAsync(l, "UPSERT"));
            }
            await _db.SaveChangesAsync();
            await _hook.OnLocationPublishedAsync(batch, Guid.NewGuid());

            if (tx != null) await tx.CommitAsync();
            return locs.Count;
        }
        finally
        {
            if (tx != null) await tx.DisposeAsync();
        }
    }
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~LocationPublishServiceTests"` → 全 PASS
Run: `dotnet test CP6.Tests/CP6.Tests.csproj` → 1535 passed / 5 skipped

- [ ] **Step 5: Commit**

```bash
git add CP6.Core/Services/Space/ILocationPublishService.cs CP6.Core/Services/Space/LocationPublishService.cs CP6.Tests/LocationPublishServiceTests.cs
git commit -m "feat(space): RepublishAsync 路径元数据再发布基元（ch04 §7.2 路径B，码冻结只升版）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: H4 改挂自动 re-publish（SceneService 检测 ZoneId/AisleId 变更）

**Files:**
- Modify: `CP6.Core/Services/Space/SceneService.cs`（ctor 3 参 + rack update 块检测 + 保存后触发）
- Test: `CP6.Tests/SceneServiceTests.cs`（`Make()` 帮手改造 + 新增 2 个测试）

**Interfaces:**
- Consumes: Task 1 `RepublishAsync`（在场景保存事务内调用——嵌套守卫使其加入外层事务）
- Produces: `SceneService` 构造函数变 3 参 `(CP6Context db, LocationGeometryService geo, ILocationPublishService publish)`——DI 自动解析（Program.cs 注册不用改），**测试帮手须同步**。行为：场景保存中货架 `ZoneId` 或 `AisleId` 变更 → 该货架下 Status=1 库位自动 re-publish（草稿不动）。

- [ ] **Step 1: 改测试帮手 + 写失败测试**

`CP6.Tests/SceneServiceTests.cs`：

① `Make()` 改为（using 需补 `CP6.Core.Services.Integration` / `Microsoft.Extensions.Logging.Abstractions`）：

```csharp
    private static (CP6Context db, SceneService svc) Make()
    {
        var db = new CP6Context(new DbContextOptionsBuilder<CP6Context>()
            .UseInMemoryDatabase(Guid.NewGuid().ToString()).Options);
        var geo = new LocationGeometryService(db);
        var publish = new LocationPublishService(db, new TenantContext(), new CodeEngineService(db),
            new SpaceBridgeHook(db, NullLogger<SpaceBridgeHook>.Instance, new NoOpWmsLocationConsumer()),
            new StubWmsStockQuery(), new CP6.Core.Services.Wms.WmsBinDeactivator(db));
        return (db, new SceneService(db, geo, publish));
    }
```

② 文件末尾新增 2 个测试：

```csharp
    [Fact]
    public async Task SaveScene_RackZoneChanged_RepublishesPublishedLocations()
    {
        // H4（ch04 §7.2 路径B）：层级归属变更 → 已发布库位自动 re-publish 刷新 WMS path
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var site = new Space_Site { Id = Guid.NewGuid(), SiteCode = "WH1", SiteName = "S1" };
        var floor = new Space_Floor { Id = floorId, SiteId = site.Id, Level = 1, FloorCode = "F1", FloorName = "F1" };
        var zoneA = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "ZA", ZoneName = "A" };
        var zoneB = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "ZB", ZoneName = "B" };
        var rack = new Space_Rack { Id = Guid.NewGuid(), ZoneId = zoneA.Id, FloorId = floorId, RackCode = "R1", Cols = 1, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000 };
        var pubLoc = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Placed = true, Status = 1, CodeOrigin = 1, LocationCode = "ZA-01", Col = 1, Level = 1, Depth = 1, Version = 1 };
        var draftLoc = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Placed = true, Status = 0, CodeOrigin = 1, LocationCode = null, Col = 1, Level = 1, Depth = 1 };
        db.AddRange(site, floor, zoneA, zoneB, rack, pubLoc, draftLoc);
        await db.SaveChangesAsync();
        var rowVersion = rack.RowVersion;

        await svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Racks = new List<RackDto>
            {
                new RackDto
                {
                    Id = rack.Id, ZoneId = zoneB.Id, AisleId = null, RackCode = "R1",
                    X = 0, Y = 0, Z = 0, RotationZ = 0,
                    Cols = 1, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000,
                    Enable = true, RowVersion = rowVersion
                }
            }
        }, "u");

        var loc = await db.Space_Locations.SingleAsync(l => l.Id == pubLoc.Id);
        Assert.Equal(2, loc.Version);                       // re-publish 升版
        Assert.Equal(1, loc.Status);                        // 状态不变
        var evt = await db.IntegrationEvents.SingleAsync();
        var payload = System.Text.Json.JsonSerializer.Deserialize<System.Text.Json.JsonElement>(evt.PayloadJson);
        var item = payload.GetProperty("Items")[0];
        Assert.Equal("UPSERT", item.GetProperty("Op").GetString());
        Assert.Equal("ZB", item.GetProperty("Path").GetProperty("ZoneCode").GetString());   // 新归属
        // 草稿库位不受波及
        Assert.Equal(0, (await db.Space_Locations.SingleAsync(l => l.Id == draftLoc.Id)).Version);
    }

    [Fact]
    public async Task SaveScene_GeometryOnlyChange_NoRepublish()
    {
        // D4：纯几何（挪位/旋转/改尺寸不缩格）不发布——join key 不漂移
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var zone = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "Z1", ZoneName = "Z" };
        var rack = new Space_Rack { Id = Guid.NewGuid(), ZoneId = zone.Id, FloorId = floorId, RackCode = "R1", Cols = 1, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000 };
        db.AddRange(zone, rack);
        db.Space_Locations.Add(new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Placed = true, Status = 1, CodeOrigin = 1, LocationCode = "Z1-01", Col = 1, Level = 1, Depth = 1, Version = 1 });
        await db.SaveChangesAsync();

        await svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Racks = new List<RackDto>
            {
                new RackDto
                {
                    Id = rack.Id, ZoneId = zone.Id, AisleId = null, RackCode = "R1",
                    X = 5000, Y = 3000, Z = 0, RotationZ = 90,   // 只挪位旋转
                    Cols = 1, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000,
                    Enable = true, RowVersion = rack.RowVersion
                }
            }
        }, "u");

        Assert.Equal(1, (await db.Space_Locations.SingleAsync()).Version);   // 版本不动
        Assert.Equal(0, await db.IntegrationEvents.CountAsync());            // 零事件
    }
```

（若 `RackDto` 字段名与上述不符——如 `DepthCount`/`RowVersion` 命名——以 `CP6.Entity/DTOs/Space/` 实际 DTO 为准微调测试种子，断言语义不变；在报告中说明。）

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~SceneServiceTests"`
Expected: FAIL——编译错（SceneService 2 参构造）。

- [ ] **Step 3: 实现**

`SceneService.cs`：

① ctor：

```csharp
    private readonly CP6Context _db;
    private readonly LocationGeometryService _geo;
    private readonly ILocationPublishService _publish;

    public SceneService(CP6Context db, LocationGeometryService geo, ILocationPublishService publish)
    {
        _db  = db;
        _geo = geo;
        _publish = publish;
    }
```

② `SaveSceneAsync` 内 `var changedRackIds = new HashSet<Guid>();` 之后加一行：

```csharp
            var pathChangedRackIds = new HashSet<Guid>();   // H4：层级归属（ZoneId/AisleId）变更的货架
```

③ rack update 块（`if (existing != null)` 分支）在「位姿/尺寸变更检测」之前加：

```csharp
                    // H4（ch04 §7.2 路径B）：层级归属变更检测——纯几何不发布，但 path 归属变更须 re-publish
                    if (existing.ZoneId != rd.ZoneId || existing.AisleId != rd.AisleId)
                        pathChangedRackIds.Add(existing.Id);
```

④ 几何重算循环（`foreach (var rid in changedRackIds)`）之后、`if (tx != null) await tx.CommitAsync();` 之前加：

```csharp
            // ── H4：改挂货架下的已发布库位 re-publish（在本事务内，RepublishAsync 嵌套守卫自动加入）──
            if (pathChangedRackIds.Count > 0)
            {
                var republishIds = await _db.Space_Locations
                    .Where(l => l.RackId != null && pathChangedRackIds.Contains(l.RackId.Value) && l.Status == 1)
                    .Select(l => l.Id)
                    .ToListAsync();
                if (republishIds.Count > 0)
                    await _publish.RepublishAsync(republishIds, user);
            }
```

- [ ] **Step 4: 跑 Scene 全套 + 全量回归**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~SceneServiceTests|FullyQualifiedName~SceneIoServiceTests|FullyQualifiedName~BindCodesTests"` → 全 PASS
Run: `dotnet test CP6.Tests/CP6.Tests.csproj` → 1537 passed / 5 skipped

- [ ] **Step 5: Commit**

```bash
git add CP6.Core/Services/Space/SceneService.cs CP6.Tests/SceneServiceTests.cs
git commit -m "feat(space): 货架改挂库区/巷道自动 re-publish（评审 H4，ch04 §7.2 路径B）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: H2 缩格护栏（已发布 Restrict + 幽灵草稿/停用连带删）

**Files:**
- Modify: `CP6.Core/Services/Space/SceneService.cs`（rack update 块加缩格检测）
- Test: `CP6.Tests/SceneServiceTests.cs`（新增 2 个测试）

**Interfaces:**
- Consumes: 无新依赖
- Produces: 行为——缩格（Cols/Levels/DepthCount 变小）时：越界库位含 Status=1 → 抛 `E-SPACE-403: 缩格触及已发布库位，请先停用越界库位再缩格`（整次保存回滚）；越界库位全为 Status=0/2 → 连带 Remove（幽灵位不再产生）。

- [ ] **Step 1: 写失败测试**

`CP6.Tests/SceneServiceTests.cs` 末尾追加（复用 Task 2 的 DTO 种子写法）：

```csharp
    [Fact]
    public async Task SaveScene_ShrinkRack_PublishedOutOfBounds_Throws403_NothingSaved()
    {
        // H2（2026-07-06 拍板：Restrict 阻断，非契约§4字面的自动停用）
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var zone = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "Z1", ZoneName = "Z" };
        var rack = new Space_Rack { Id = Guid.NewGuid(), ZoneId = zone.Id, FloorId = floorId, RackCode = "R1", Cols = 3, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000 };
        db.AddRange(zone, rack);
        db.Space_Locations.Add(new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Placed = true, Status = 1, CodeOrigin = 1, LocationCode = "Z1-03", Col = 3, Level = 1, Depth = 1, Version = 1 });
        await db.SaveChangesAsync();

        var dto = new SceneSaveDto
        {
            Racks = new List<RackDto>
            {
                new RackDto
                {
                    Id = rack.Id, ZoneId = zone.Id, AisleId = null, RackCode = "R1",
                    X = 0, Y = 0, Z = 0, RotationZ = 0,
                    Cols = 2, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000,   // 3→2 缩格
                    Enable = true, RowVersion = rack.RowVersion
                }
            }
        };

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => svc.SaveSceneAsync(floorId, dto, "u"));
        Assert.StartsWith("E-SPACE-403", ex.Message);
        Assert.Equal(3, (await db.Space_Racks.AsNoTracking().SingleAsync()).Cols);   // 缩格未生效
        Assert.Equal(1, await db.Space_Locations.CountAsync());                      // 库位仍在
    }

    [Fact]
    public async Task SaveScene_ShrinkRack_DraftOutOfBounds_CascadeDeleted()
    {
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var zone = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "Z1", ZoneName = "Z" };
        var rack = new Space_Rack { Id = Guid.NewGuid(), ZoneId = zone.Id, FloorId = floorId, RackCode = "R1", Cols = 3, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000 };
        db.AddRange(zone, rack);
        var inBounds = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Placed = true, Status = 0, CodeOrigin = 1, Col = 1, Level = 1, Depth = 1 };
        var ghost = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Placed = true, Status = 0, CodeOrigin = 1, Col = 3, Level = 1, Depth = 1 };
        db.AddRange(inBounds, ghost);
        await db.SaveChangesAsync();

        await svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Racks = new List<RackDto>
            {
                new RackDto
                {
                    Id = rack.Id, ZoneId = zone.Id, AisleId = null, RackCode = "R1",
                    X = 0, Y = 0, Z = 0, RotationZ = 0,
                    Cols = 2, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000,
                    Enable = true, RowVersion = rack.RowVersion
                }
            }
        }, "u");

        Assert.Equal(2, (await db.Space_Racks.SingleAsync()).Cols);
        var remaining = await db.Space_Locations.ToListAsync();
        Assert.Single(remaining);                                   // 幽灵位已连带删
        Assert.Equal(inBounds.Id, remaining[0].Id);
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~SaveScene_ShrinkRack"`
Expected: 2 个 FAIL（当前缩格只改字段，幽灵位留存、已发布不拦）。

- [ ] **Step 3: 实现**

`SceneService.cs` rack update 块，在 H4 检测（Task 2 的③）之后、「位姿/尺寸变更检测」之前加：

```csharp
                    // H2 缩格护栏（2026-07-06 拍板：Restrict）：越界库位——已发布阻断，草稿/停用连带删
                    if (rd.Cols < existing.Cols || rd.Levels < existing.Levels || rd.DepthCount < existing.DepthCount)
                    {
                        var outOfBounds = await _db.Space_Locations
                            .Where(l => l.RackId == existing.Id &&
                                        (l.Col > rd.Cols || l.Level > rd.Levels || l.Depth > rd.DepthCount))
                            .ToListAsync();
                        if (outOfBounds.Any(l => l.Status == 1))
                            throw new InvalidOperationException(
                                "E-SPACE-403: 缩格触及已发布库位，请先停用越界库位再缩格");
                        if (outOfBounds.Count > 0)
                            _db.Space_Locations.RemoveRange(outOfBounds);   // 幽灵位清理（H2）
                    }
```

（`Space_Rack.DepthCount` 字段名以实体为准；若实体里是别名则同步。`l.Col` 等为 `int?`，null（未落位）不参与越界比较——SQL 语义天然满足。）

- [ ] **Step 4: 跑 Scene 全套 + 全量回归**

Run: 过滤 SceneServiceTests → 全 PASS；全量 → 1539 passed / 5 skipped

- [ ] **Step 5: Commit**

```bash
git add CP6.Core/Services/Space/SceneService.cs CP6.Tests/SceneServiceTests.cs
git commit -m "fix(space): 缩格护栏——已发布越界 Restrict 阻断+幽灵草稿连带删（评审 H2）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: 库位删除通道（Deletes.Locations）+ 场景删货架契约对齐

**Files:**
- Modify: `CP6.Entity/DTOs/Space/SceneSaveDto.cs`（`Deletes` 加 Locations）
- Modify: `CP6.Core/Services/Space/SceneService.cs`（Deletes 块：新增 Locations 处理 + Racks 处理改契约语义）
- Test: `CP6.Tests/SceneServiceTests.cs`（新增 3 个测试；**既有「删有库位货架→E-SPACE-003」测试改断言**）

**Interfaces:**
- Consumes: 无新依赖
- Produces: `Deletes.Locations`（`List<Guid>?`）——Status=0/2 可删、Status=1 抛 `E-SPACE-406: 已发布库位不可删除，请先停用`（**取号前 grep 确认 406 未占用，冲突则顺延 408 并报告**）；场景删货架从「有任何库位全拦 E-003」改为契约 §7.1 语义：「有 Status=1 → E-SPACE-403；其余 → 库位级联删+删货架」。

- [ ] **Step 1: 写失败测试**

`CP6.Tests/SceneServiceTests.cs` 末尾追加：

```csharp
    [Fact]
    public async Task SaveScene_DeleteLocation_DraftAndDeactivated_Allowed_PublishedRejected()
    {
        // 库位删除通道（2026-07-06 拍板：0/2 可删，1 拒绝。停用位删除后码仍占 T_WmsBin 锚——已知代价）
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var draft = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, Status = 0 };
        var deact = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, Status = 2, LocationCode = "X-01", Version = 2 };
        var pub = new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, Status = 1, LocationCode = "P-01", Version = 1 };
        db.AddRange(draft, deact, pub);
        await db.SaveChangesAsync();

        // 草稿+停用：删除成功
        await svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Deletes = new Deletes { Locations = new List<Guid> { draft.Id, deact.Id } }
        }, "u");
        Assert.Equal(1, await db.Space_Locations.CountAsync());

        // 已发布：拒绝
        var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Deletes = new Deletes { Locations = new List<Guid> { pub.Id } }
        }, "u"));
        Assert.StartsWith("E-SPACE-406", ex.Message);
        Assert.Equal(1, await db.Space_Locations.CountAsync());
    }

    [Fact]
    public async Task SaveScene_DeleteRack_DraftLocationsOnly_Cascades()
    {
        // ch04 §7.1：全草稿 → 连带删（替代旧 E-003 全拦）
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var zone = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "Z1", ZoneName = "Z" };
        var rack = new Space_Rack { Id = Guid.NewGuid(), ZoneId = zone.Id, FloorId = floorId, RackCode = "R1", Cols = 1, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000 };
        db.AddRange(zone, rack);
        db.Space_Locations.Add(new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Status = 0, Col = 1, Level = 1, Depth = 1 });
        await db.SaveChangesAsync();

        await svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Deletes = new Deletes { Racks = new List<Guid> { rack.Id } }
        }, "u");

        Assert.Equal(0, await db.Space_Racks.CountAsync());
        Assert.Equal(0, await db.Space_Locations.CountAsync());
    }

    [Fact]
    public async Task SaveScene_DeleteRack_WithPublishedLocation_Throws403()
    {
        var (db, svc) = Make();
        var floorId = Guid.NewGuid();
        var zone = new Space_Zone { Id = Guid.NewGuid(), FloorId = floorId, ZoneCode = "Z1", ZoneName = "Z" };
        var rack = new Space_Rack { Id = Guid.NewGuid(), ZoneId = zone.Id, FloorId = floorId, RackCode = "R1", Cols = 1, Levels = 1, DepthCount = 1, CellW = 1000, CellH = 1000, CellD = 1000 };
        db.AddRange(zone, rack);
        db.Space_Locations.Add(new Space_Location { Id = Guid.NewGuid(), FloorId = floorId, RackId = rack.Id, Status = 1, LocationCode = "Z1-01", Col = 1, Level = 1, Depth = 1, Version = 1 });
        await db.SaveChangesAsync();

        var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => svc.SaveSceneAsync(floorId, new SceneSaveDto
        {
            Deletes = new Deletes { Racks = new List<Guid> { rack.Id } }
        }, "u"));
        Assert.StartsWith("E-SPACE-403", ex.Message);
        Assert.Equal(1, await db.Space_Racks.CountAsync());
    }
```

同时找到既有断言 E-SPACE-003 的删货架测试（文件头注释提到「删有库位货架→E-003」），把它改为与新语义一致（种子若是草稿库位→改断言级联删成功；若要保留 Restrict 断言→把种子改成 Status=1 并断言 E-SPACE-403），commit message 注明语义变更来源（契约 §7.1 + 2026-07-06 拍板）。

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~SaveScene_Delete"`
Expected: FAIL——编译错（`Deletes.Locations` 不存在）。

- [ ] **Step 3: 实现**

① `SceneSaveDto.cs` 的 `Deletes` 类加：

```csharp
    /// <summary>库位删除（2026-07-06 拍板：Status=0/2 可删、=1 拒绝 E-SPACE-406）</summary>
    public List<Guid>? Locations { get; set; }
```

② `SceneService.cs` Deletes 块，在 Racks 循环**之前**插入：

```csharp
            foreach (var locId in dto.Deletes?.Locations ?? new List<Guid>())
            {
                var e = await _db.Space_Locations.FirstOrDefaultAsync(l => l.Id == locId);
                if (e == null) continue;
                // 已发布不可删（须先停用）；草稿/停用可删（2026-07-06 拍板）。
                // 注意：停用位删除后其码仍被 T_WmsBin 停用行占据 join 锚，同码新库位发布会被
                // 锚碰撞 REJECTED——锚清理机制记后续票，此为拍板时已知代价。
                if (e.Status == 1)
                    throw new InvalidOperationException("E-SPACE-406: 已发布库位不可删除，请先停用");
                _db.Space_Locations.Remove(e);
            }
```

③ Racks 删除循环整体替换：

```csharp
            foreach (var rackId in dto.Deletes?.Racks ?? new List<Guid>())
            {
                // ch04 §7.1：有已发布库位 → Restrict；全草稿/停用 → 库位连带删（旧 E-003 全拦废止）
                if (await _db.Space_Locations.AnyAsync(l => l.RackId == rackId && l.Status == 1))
                    throw new InvalidOperationException("E-SPACE-403: 该货架下有已发布库位，请先停用");
                var children = await _db.Space_Locations.Where(l => l.RackId == rackId).ToListAsync();
                if (children.Count > 0) _db.Space_Locations.RemoveRange(children);
                var e = await _db.Space_Racks.FirstOrDefaultAsync(r => r.Id == rackId);
                if (e != null) _db.Space_Racks.Remove(e);
            }
```

- [ ] **Step 4: 跑 Scene 全套（含改写的旧测试）+ 全量回归**

Run: 过滤 SceneServiceTests → 全 PASS；全量 → 1542 passed / 5 skipped（按 +3 新测试；旧测试改写不改数量）

- [ ] **Step 5: Commit**

```bash
git add CP6.Entity/DTOs/Space/SceneSaveDto.cs CP6.Core/Services/Space/SceneService.cs CP6.Tests/SceneServiceTests.cs
git commit -m "feat(space): 库位删除通道（草稿/停用可删）+ 场景删货架对齐契约§7.1（评审 H2 配套）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: 删巷道护栏 + mode=deactivate|rehome（H3 主体）

**Files:**
- Modify: `CP6.Core/Services/Space/ISpaceMasterService.cs`（`DeleteAisleAsync` 签名）
- Modify: `CP6.Core/Services/Space/SpaceMasterService.cs`（ctor 3 参 + `DeleteAisleAsync` 重写）
- Modify: `CP6.WebApi/Controllers/Space/SpaceMasterController.cs:124-129`（端点加 query 参数）
- Test: `CP6.Tests/SpaceMasterServiceTests.cs`（构造帮手同步 + 新增 4 个测试）

**Interfaces:**
- Consumes: Task 1 `RepublishAsync`、波1 `DeactivateAsync`
- Produces:
  ```csharp
  Task DeleteAisleAsync(Guid id, string? mode = null, Guid? targetAisleId = null, string? user = null);
  ```
  默认参数——既有调用点编译不破坏。`SpaceMasterService` 构造函数变 3 参 `(CP6Context db, LocationGeometryService geo, ILocationPublishService publish)`（**类头 v1.1 注释「构造只注入 db+geo」同步改写**；DI 自动解析；测试帮手须同步——SpaceMasterServiceTests 的构造处照 SceneServiceTests Task 2 的 `Make()` 组装 publish）。

- [ ] **Step 1: 写失败测试**

`CP6.Tests/SpaceMasterServiceTests.cs`：先把该文件构造 `SpaceMasterService` 的帮手升级为 3 参（组装方式同 Task 2 的 Make()），再新增：

```csharp
    [Fact]
    public async Task DeleteAisle_WithPublishedLocations_NoMode_Throws402()
    {
        // 种子：site→floor→zone→aisle→rack(挂aisle)→published loc
        // （按本文件既有种子写法组装；rack.AisleId = aisle.Id；loc.Status=1 有码）
        // 断言：InvalidOperationException 且 StartsWith "E-SPACE-402"；aisle 仍在；rack.AisleId 未被置空
    }

    [Fact]
    public async Task DeleteAisle_ModeDeactivate_DeactivatesThenDeletes()
    {
        // 同上种子（无库存，WmsBinDeactivator 会放行）
        // svc.DeleteAisleAsync(aisle.Id, mode: "deactivate", user: "u");
        // 断言：loc.Status==2 且 Version+1；aisle 已删；rack.AisleId==null；DEACTIVATE 事件已发（IntegrationEvents≥1）
    }

    [Fact]
    public async Task DeleteAisle_ModeRehome_RepointsRacks_Republishes()
    {
        // 种子：同 zone 下两条 aisle（src/target）；rack 挂 src；published loc Version=1
        // svc.DeleteAisleAsync(src.Id, mode: "rehome", targetAisleId: target.Id, user: "u");
        // 断言：rack.AisleId==target.Id；loc.Version==2 且 Status==1；UPSERT 事件 path.AisleCode==target 的码；src 已删
    }

    [Fact]
    public async Task DeleteAisle_ModeRehome_TargetInDifferentZone_Throws()
    {
        // 种子：target aisle 在另一 zone
        // 断言：InvalidOperationException（E-SPACE-005 系错误码）；src 仍在；rack.AisleId 未变
    }
```

（测试体按该文件既有种子/断言风格写实——上述为行为契约，字段组装照本文件已有测试抄。全部要有真实库表断言。）

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~DeleteAisle_"`
Expected: FAIL——编译错（签名/构造不匹配）。

- [ ] **Step 3: 实现**

① `ISpaceMasterService.cs:33` 改为：

```csharp
    /// <summary>
    /// 删巷道（ch04 §7.1/§7.2）：其下有已发布库位时默认 Restrict（E-SPACE-402）；
    /// mode=deactivate → 逐个走停用同步 RPC 后删（路径A）；
    /// mode=rehome → 货架改挂 targetAisleId（可 null=脱巷道）+ re-publish 刷新 path 后删（路径B）。
    /// 无已发布库位时行为同旧版：SetNull 其下 Rack.AisleId → 删。
    /// </summary>
    Task DeleteAisleAsync(Guid id, string? mode = null, Guid? targetAisleId = null, string? user = null);
```

② `SpaceMasterService.cs` ctor：

```csharp
    private readonly CP6Context _db;
    private readonly LocationGeometryService _geo;
    private readonly ILocationPublishService _publish;

    public SpaceMasterService(CP6Context db, LocationGeometryService geo, ILocationPublishService publish)
    {
        _db  = db;
        _geo = geo;
        _publish = publish;
    }
```

（类头 v1.1 注释第一条改为「构造注入 CP6Context + LocationGeometryService + ILocationPublishService（删除放行路径/改挂 re-publish 用），不注入任何租户上下文」。）

③ `DeleteAisleAsync` 整体替换：

```csharp
    public async Task DeleteAisleAsync(Guid id, string? mode = null, Guid? targetAisleId = null, string? user = null)
    {
        var aisle = await _db.Space_Aisles.FirstOrDefaultAsync(x => x.Id == id);
        if (aisle == null) return;

        var racks = await _db.Space_Racks.Where(r => r.AisleId == id).ToListAsync();
        var rackIds = racks.Select(r => r.Id).ToList();
        var published = await _db.Space_Locations
            .Where(l => l.RackId != null && rackIds.Contains(l.RackId.Value) && l.Status == 1)
            .Select(l => l.Id)
            .ToListAsync();

        if (published.Count > 0)
        {
            switch (mode)
            {
                case "deactivate":
                    // 路径A（ch04 §7.2）：逐个走停用同步 RPC。不包事务——停用是同步决策模型，
                    // 部分完成时已停用的保持停用（安全方向），重试幂等收敛。
                    foreach (var locId in published)
                        await _publish.DeactivateAsync(locId, user);
                    break;

                case "rehome":
                    // 路径B（ch04 §7.2）：改挂目标巷道（null=脱巷道也是 path 变更）→ re-publish → 删。
                    if (targetAisleId != null)
                    {
                        var target = await _db.Space_Aisles.FirstOrDefaultAsync(a => a.Id == targetAisleId.Value)
                                     ?? throw new InvalidOperationException("E-SPACE-005: 目标巷道不存在");
                        if (racks.Any(r => r.ZoneId != target.ZoneId))
                            throw new InvalidOperationException("E-SPACE-005: 目标巷道与货架不在同一库区");
                    }
                    foreach (var r in racks) r.AisleId = targetAisleId;
                    await _db.SaveChangesAsync();                    // 先落改挂，BuildItemAsync 才拼得出新 path
                    await _publish.RepublishAsync(published, user);
                    _db.Space_Aisles.Remove(aisle);                  // 货架已不挂本巷道，直接删
                    await _db.SaveChangesAsync();
                    return;

                default:
                    throw new InvalidOperationException(
                        $"E-SPACE-402: 该巷道下有 {published.Count} 个已发布库位，不能直接删除（可用 mode=deactivate|rehome）");
            }
        }

        // 默认路径（无已发布 / deactivate 后落到这里）——SetNull 保留货架。
        // 注：deactivate 后库位已 Status=2，其 WMS bin 已带最终态，path 陈旧无消费方，不再 re-publish。
        foreach (var r in racks) r.AisleId = null;
        _db.Space_Aisles.Remove(aisle);
        await _db.SaveChangesAsync();
    }
```

（`E-SPACE-005` 取号前 grep 确认；若已占用改用未占用号并报告。）

④ `SpaceMasterController.cs` DeleteAisle 端点：

```csharp
    [HttpDelete("aisle/{id:guid}")]
    public async Task<IActionResult> DeleteAisle(Guid id, [FromQuery] string? mode = null, [FromQuery] Guid? targetAisleId = null)
    {
        try { await _svc.DeleteAisleAsync(id, mode, targetAisleId, CurrentUser); return Ok2(); }
        catch (InvalidOperationException e) { return BadRequest(new { code = 400, message = e.Message }); }
    }
```

（`CurrentUser`：若该控制器尚无此属性，照 `LocationPublishController.cs:28` 加 `private string? CurrentUser => User?.Identity?.Name;`；其余端点 user 传递方式保持现状不动。）

- [ ] **Step 4: 跑测试 + 全量回归**

Run: 过滤 SpaceMasterServiceTests → 全 PASS；全量 → 1546 passed / 5 skipped（+4）

- [ ] **Step 5: Commit**

```bash
git add CP6.Core/Services/Space/ISpaceMasterService.cs CP6.Core/Services/Space/SpaceMasterService.cs CP6.WebApi/Controllers/Space/SpaceMasterController.cs CP6.Tests/SpaceMasterServiceTests.cs
git commit -m "feat(space): 删巷道发布护栏+mode=deactivate|rehome 放行路径（评审 H3，ch04 §7.1/7.2）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: 删货架 mode=deactivate|rehome + 契约对齐（H3 收尾）

**Files:**
- Modify: `CP6.Core/Services/Space/ISpaceMasterService.cs`（`DeleteRackAsync` 签名）
- Modify: `CP6.Core/Services/Space/SpaceMasterService.cs`（`DeleteRackAsync` 重写）
- Modify: `CP6.WebApi/Controllers/Space/SpaceMasterController.cs:151-156`（端点加参）
- Test: `CP6.Tests/SpaceMasterServiceTests.cs`（新增 4 个测试；**既有 DeleteRack E-003 测试改断言**——同 Task 4 预告）

**Interfaces:**
- Consumes: Task 1 `RepublishAsync`、`DeactivateAsync`、`LocationGeometryService.RecalcRackLocationsAsync(Guid rackId)`（既有，SceneService:280 同款调用）
- Produces:
  ```csharp
  Task DeleteRackAsync(Guid id, string? mode = null, Guid? targetRackId = null, string? user = null);
  ```
  语义：有 Status=1 库位——默认 `E-SPACE-403`；mode=deactivate 逐个停用后**级联删**（停用位可删，拍板②）；mode=rehome 整架库位改挂目标货架（同规格换架场景：目标网格 ≥ 源 且 无自有库位）+ 几何回填 + re-publish + 删源架。无 Status=1——直接级联删库位+货架（旧 E-003 全拦废止）。

- [ ] **Step 1: 写失败测试**

`CP6.Tests/SpaceMasterServiceTests.cs` 新增（写实规则同 Task 5 Step 1）：

```csharp
    [Fact]
    public async Task DeleteRack_DraftLocationsOnly_Cascades()
    {
        // 种子：rack + 2 个草稿库位 → DeleteRackAsync(rack.Id)
        // 断言：rack 与库位全删（旧 E-003 全拦废止）
    }

    [Fact]
    public async Task DeleteRack_WithPublished_NoMode_Throws403()
    {
        // 断言：E-SPACE-403；rack 与库位原样
    }

    [Fact]
    public async Task DeleteRack_ModeDeactivate_DeactivatesThenCascades()
    {
        // 种子：published loc（无库存）→ DeleteRackAsync(rack.Id, "deactivate", null, "u")
        // 断言：rack 已删；库位已删（停用后级联，拍板②）；停用 DEACTIVATE 事件在（IntegrationEvents≥1）；
        //       T_WmsBin 对应行 IsActive=false（真 WmsBinDeactivator 组装时）
    }

    [Fact]
    public async Task DeleteRack_ModeRehome_MovesLocations_Republishes()
    {
        // 种子：源架(RackCode=R1, 1x1x1) + published loc(Version=1, Col=1) + 目标架(RackCode=R2, 同规格, 无库位, 同zone)
        // DeleteRackAsync(src.Id, "rehome", target.Id, "u")
        // 断言：loc.RackId==target.Id；Version==2；Status==1；UPSERT 事件 path.RackCode=="R2"；源架已删
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj --filter "FullyQualifiedName~DeleteRack_"`
Expected: FAIL——编译错（签名不匹配）。

- [ ] **Step 3: 实现**

① `ISpaceMasterService.cs:39` 改为：

```csharp
    /// <summary>
    /// 删货架（ch04 §7.1/§7.2）：有已发布库位默认 Restrict（E-SPACE-403）；
    /// mode=deactivate → 逐个停用后级联删（停用位可删，2026-07-06 拍板）；
    /// mode=rehome → 整架库位改挂 targetRackId（同规格换架：目标网格≥源且无自有库位）+ re-publish 后删源架。
    /// 无已发布库位 → 库位级联删 + 删货架（旧 E-SPACE-003 全拦废止）。
    /// </summary>
    Task DeleteRackAsync(Guid id, string? mode = null, Guid? targetRackId = null, string? user = null);
```

② `SpaceMasterService.DeleteRackAsync` 整体替换：

```csharp
    public async Task DeleteRackAsync(Guid id, string? mode = null, Guid? targetRackId = null, string? user = null)
    {
        var rack = await _db.Space_Racks.FirstOrDefaultAsync(x => x.Id == id);
        if (rack == null) return;

        var published = await _db.Space_Locations
            .Where(l => l.RackId == id && l.Status == 1)
            .Select(l => l.Id)
            .ToListAsync();

        if (published.Count > 0)
        {
            switch (mode)
            {
                case "deactivate":
                    // 路径A：逐个停用（同步 RPC 决策模型，不包事务，部分完成重试幂等）→ 落到下方级联删
                    foreach (var locId in published)
                        await _publish.DeactivateAsync(locId, user);
                    break;

                case "rehome":
                    // 路径B（同规格换架）：目标网格 ≥ 源、且无自有库位（否则格口冲突）
                    if (targetRackId == null)
                        throw new InvalidOperationException("E-SPACE-002: mode=rehome 需要 targetRackId");
                    var target = await _db.Space_Racks.FirstOrDefaultAsync(r => r.Id == targetRackId.Value)
                                 ?? throw new InvalidOperationException("E-SPACE-002: 目标货架不存在");
                    if (target.Cols < rack.Cols || target.Levels < rack.Levels || target.DepthCount < rack.DepthCount)
                        throw new InvalidOperationException("E-SPACE-002: 目标货架网格小于源货架，无法改挂");
                    if (await _db.Space_Locations.AnyAsync(l => l.RackId == target.Id))
                        throw new InvalidOperationException("E-SPACE-002: 目标货架已有库位，无法改挂");

                    var movable = await _db.Space_Locations.Where(l => l.RackId == id).ToListAsync();
                    foreach (var l in movable)
                    {
                        l.RackId  = target.Id;
                        l.FloorId = target.FloorId;
                        l.Modifier = user;
                        l.ModifyDate = DateTime.Now;
                    }
                    await _db.SaveChangesAsync();                         // 先落改挂
                    await _geo.RecalcRackLocationsAsync(target.Id);       // 几何回填（纯几何不发布，D4）
                    await _publish.RepublishAsync(published, user);       // path.rackCode 变 → re-publish（§7.2 B）
                    _db.Space_Racks.Remove(rack);
                    await _db.SaveChangesAsync();
                    return;

                default:
                    throw new InvalidOperationException(
                        "E-SPACE-403: 该货架下有已发布库位，请先停用（或 mode=deactivate|rehome）");
            }
        }

        // 无已发布（或 deactivate 后）→ 库位级联删 + 删货架（停用位可删，2026-07-06 拍板；
        // 其码仍占 T_WmsBin 锚，同码再发布会被 REJECTED——锚清理记后续票）
        var children = await _db.Space_Locations.Where(l => l.RackId == id).ToListAsync();
        if (children.Count > 0) _db.Space_Locations.RemoveRange(children);
        _db.Space_Racks.Remove(rack);
        await _db.SaveChangesAsync();
    }
```

③ Controller DeleteRack 端点加 `[FromQuery] string? mode = null, [FromQuery] Guid? targetRackId = null` 并传递（写法同 Task 5 ④）。

④ 既有 DeleteRack E-003 测试按 Task 4 预告改断言。

- [ ] **Step 4: 跑测试 + 全量回归**

Run: 过滤 SpaceMasterServiceTests → 全 PASS；全量 → 1550 passed / 5 skipped（+4）

- [ ] **Step 5: Commit**

```bash
git add CP6.Core/Services/Space/ISpaceMasterService.cs CP6.Core/Services/Space/SpaceMasterService.cs CP6.WebApi/Controllers/Space/SpaceMasterController.cs CP6.Tests/SpaceMasterServiceTests.cs
git commit -m "feat(space): 删货架护栏+mode=deactivate|rehome（同规格换架）+ 契约§7.1级联语义（评审 H3 收尾）

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: 契约 v1.2 文档补丁 + 全量回归收尾

**Files:**
- Modify: `docs/space/04-publish-contract.md`（文首补丁摘要 + 对应章节追记）
- 无产品代码变更

**Interfaces:** 无——文档任务。**这是终审的两条计划侧备忘的兑现**：①「消费端持久化失败语义」成文；② WarehouseCd 长度域成文。

- [ ] **Step 1: 文档追记（v1.2 补丁）**

`docs/space/04-publish-contract.md` 文首 v1.1 摘要之后加 v1.2 摘要块，并在对应章节就地追记（每处标「(v1.2实装补丁)」）：

1. **§5.1 墓碑修正**：DEACTIVATE 遇 bin 不存在时**不再跳过**，落墓碑行（IsActive=false + Version 占位）——防「UPSERT 仍在重试队列时停用 → 迟到旧版 UPSERT 复活库位」的乱序分叉；无 WarehouseCd（建不了 join 锚）才退回幂等跳过。（2026-07-05 评审 H6 拍板，波1 已实装于 WmsBinConsumer 与 WmsBinDeactivator 双入口。）
2. **§3.4 WarehouseCd 长度域**：`WarehouseCd` 域为 `nvarchar(10)`；`SiteCode` 上限 50——默认映射回退时若 SiteCode 超 10 字符，发布/停用被 `E-SPACE-405` fail-fast 拦截（波1 终审 #1 实装），多仓长站点码客户必须显式配置 `Space_Site.WarehouseCd`。
3. **新增小节「§5.4 消费端持久化失败语义」（v1.2）**：约束触发不是处理策略——① 消费端 SaveChanges 失败时必须清理自身 change tracker 写入（Detach 本次 WmsBin entries）再抛，防止毒化同 scope 的事件簿记与 Worker 整租户批尾（波1 终审 #2）；② join 锚 `(TenantId, WarehouseCd, LocationCode)` 被其他 LocationId 占用 → 业务拒绝 `REJECTED`（整事件 Failed → 重试 → DeadLetter 告警），不走异常链（波1 终审 #3）；③ 墓碑分支的锚碰撞残余走异常→重试→DeadLetter，可见收敛（记票优化）。
4. **§7 落地说明**：`DELETE /aisle/{id}` 与 `/rack/{id}` 的 `?mode=deactivate|rehome`（+`targetAisleId`/`targetRackId`）已实装（波1.5）；缩格触及已发布库位 = **Restrict 阻断**（2026-07-06 拍板，替代 §4 表「减格→DEACTIVATE」字面——原因：停用是同步 RPC 决策模型，场景保存内嵌批量停用在任一格口有库存时整存挫败，不可预期）；库位删除通道对 Status=0/2 开放、=1 拒绝 `E-SPACE-406`，停用位删除后码仍占 T_WmsBin 锚（清理机制后续票）。
5. **§11 消息表追加**：`E-SPACE-405`（站点编码超长无映射）、`E-SPACE-406`（已发布库位不可删除）、`E-SPACE-402/403` 的 mode 提示文案、rehome 校验错误（E-SPACE-005/002 系）——以代码实际取号为准登记。

- [ ] **Step 2: 全量回归 + 构建终验**

Run: `dotnet test CP6.Tests/CP6.Tests.csproj` → 预期 1550 passed / 5 skipped（波1.5 净增 18：T1×3+T2×2+T3×2+T4×3+T5×4+T6×4）
Run: `dotnet build CP6.slnx` → 0 errors

- [ ] **Step 3: Commit**

```bash
git add docs/space/04-publish-contract.md
git commit -m "docs(space): 发布契约 v1.2 补丁——墓碑修正/WarehouseCd长度域/消费端持久化失败语义/§7 mode落地与拍板记录

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## 自检记录（写计划时已核）

- **Spec 覆盖**：H2（Task 3 缩格 + Task 4 幽灵位删除通道）、H3（Task 5 巷道 / Task 6 货架，含契约 §7.1 Restrict 与 §7.2 两条放行路径完整 mode 实现）、H4（Task 1 基元 + Task 2 场景改挂自动触发）；终审计划侧备忘两条由 Task 7 兑现。契约 §4 表逐行对照：纯几何不发布（Task 2 有反证测试）/减格→Restrict（拍板替代）/增格新库位随下次发布（既有行为，前端生成草稿）/采纳绑定不发布（BindCodesAsync 未动）。
- **用户拍板三项**全部落进 Global Constraints 并在对应代码注释留痕（含拍板日期）。
- **无占位符核查**：Task 5/6 的 Step 1 测试给的是行为契约+断言清单而非全码——因 SpaceMasterServiceTests 的种子帮手风格须现场对齐（写死会与该文件既有帮手冲突）；行为断言逐条给定，实现代码全量给定，不属"写测试"式占位。其余任务代码全量。
- **类型一致性**：`RepublishAsync(IReadOnlyCollection<Guid>, string?)` Task 1 定义、Task 2/5/6 调用形参一致（`List<Guid>` 协变入参 OK）；`SceneService` 3 参构造 Task 2 定义、Task 3/4 沿用；`SpaceMasterService` 3 参构造 Task 5 定义、Task 6 沿用；两个 Delete 方法默认参数保证既有调用点（含 SceneIoService 若有）编译不破坏。嵌套事务守卫使 RepublishAsync 在「SceneService 事务内（Task 2）」与「无事务（Task 5/6）」两种环境都安全。
- **执行顺序**：严格 1→7 串行（2 依赖 1 的方法与 3 参构造；3/4 改 Task 2 之后的 SceneService；6 沿用 5 的 ctor）。
- **明确不做（记票）**：T_WmsBin 停用行/墓碑锚清理机制（拍板②已知代价）；对账漂移 job；消费端批量化；Site 主数据 WarehouseCd UI；这些与波1 遗留票合并管理（见 space3d 记忆）。
