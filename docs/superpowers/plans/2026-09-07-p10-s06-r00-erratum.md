# P10 S06 R00 Storage Erratum Implementation Plan

> **For agentic workers:** Use `superpowers:executing-plans` sequentially in the existing S06 worktree, as selected by the user. Checkboxes record actual progress.

**Goal:** Document the approved Cloudflare-native P10 identity protocol outside the immutable R00 payload without weakening legacy WMS gates.

**Architecture:** Add one prominent outside-payload notice and one scoped erratum to the existing public ADR. Preserve the original decision bytes and approval digest; use existing public-contract validation plus a reproducible exact-payload comparison. This is documentation only, not a runtime or storage behavior change.

**Tech Stack:** Markdown, PowerShell 7, existing public-contract verifier.

## Scope and baseline

- Existing S06 branch is based on current `origin/main@6f9d09f4e3b1627a25ec7859b748eba8cd66f621`; local verifier-foundation commit is `e91b73833e18111cf9c63233227d6df3d4f76c09`. Both were inspected before this addition.
- Modify only `docs/devops/adr/ADR-CRM-R00-RELEASE-AUTHORITY.md`; this plan is the only new file in this bounded step.
- Do not alter the frozen payload, its markers, approval records, workflows, deployment scripts, or status ledgers to claim S06 complete. Full S06 delivery will reconcile the required four ledgers after observed results exist.
- Official capability reference checked on 2026-09-07: [R2 S3 API compatibility](https://developers.cloudflare.com/r2/api/s3/api/). S3 versioning/Object Lock API absence is not a blanket statement about all separately named Cloudflare features.

## Task 1: Add the bounded documentation

- [x] Insert this notice immediately after the existing outside-payload introductory paragraph:

```markdown
> P10 执行说明：下方冻结载荷包含原先对 R2 VersionId/Object Lock 的假定；Cloudflare R2 的实际能力与 P10 替代协议见文末“P10 S06 Cloudflare R2 勘误”。原载荷保持逐字不变，不以该历史假定证明能力已就绪。
```

- [x] Insert this exact section after the frozen payload end marker and before the public status mirror:

```markdown
## P10 S06 Cloudflare R2 勘误（2026-09-07，不进入私有 R00 载荷）

本节落实已批准的 [P10 发布治理设计第 2、8、12–14 节](https://github.com/GTX537/CP6.Platform/blob/3ff27e26962dcfd722887afb80a4306010dd9ee1/docs/superpowers/specs/2026-09-01-p10-release-governance-design.md)。上方冻结正文保留为原决策镜像；本节没有重写或重新批准私有 R00 载荷，其摘要继续为 `64a53dd895aedc20a51288ad0ffdb69f60ddc7c22012c1df83984efba5adbc03`。

- 平台事实：截至本次核对，Cloudflare R2 的 [S3 API 兼容表](https://developers.cloudflare.com/r2/api/s3/api/) 将 S3 bucket versioning、Object Lock configuration 和对象 retention/legal-hold headers 标为未实现；`PutObject` 支持条件请求。不得把原表的 “Existing” 当作该 R2 存储权威已具备这些 S3 保证的运行证据，也不得用空值、`null` 或伪造值冒充有效 `VersionId`。
- P10 对象身份：使用预先固定的 `storageAuthority` 映射，加精确 `key + SHA-256 + byteLength + mediaType`；对象采用 content-addressed key，读取后重新校验完整字节。这里不宣称 S3 VersionId 或 S3 Object Lock 保证。
- P10 写入规则：受管对象必须满足 4 MiB 上限，使用单次 `PutObject(If-None-Match: *)` 创建，不使用 multipart，也不回退为无条件覆盖。内容寻址对象冲突时复核已有完整字节；签名 bundle 冲突时验证已有 bundle 是否签署同一预定 Locator。
- P10 权威发现：使用固定路径的签名 CandidateLocator 及相邻 sigstore bundle。先完成独立验证工作流，再上传主体/证据、签署精确 Locator 字节并通过干净的 pre-commit verifier；最后一次条件式 Locator 创建才是权威提交点。消费者先按带外固定的公钥/策略验证 Locator，再读取并验证全部必需对象；不得通过 listing、孤立对象或未限定的 latest 宣称候选已发布。
- P10 失败语义：原始 Locator 意图及其字节不能在重试中改变；只有字节相同且签名/策略检查通过才可幂等复用。冲突或 post-commit 验证失败保留原对象并废弃该候选身份，前向修正必须使用新身份。完成 post-commit 确认和跨仓审计前，不得标为 Frozen。
- 保持的边界：GitHub R2/GHCR 唯一权威、Build once/deploy many、digest 身份、签名、SBOM、漏洞扫描、真实门禁和受保护审批均不改变。本勘误不删除或绕过现有 WMS R2/Shadow 的 VersionId、Object Lock 或其他门禁；这些旧路径若缺少其所要求的真实能力，仍是 No-Go，不能借用 P10 的替代协议直接验收。
- 当前交付状态：本节只是 S06 的文档实施，不是 R2 实际写入、OCI 发布、候选冻结或生产部署证据。P10 的 `PlatformReference` 候选保持 `deployable=false`；完整验证、发布和跨仓审计仍须分别完成。
```

## Task 2: Verify the documentation boundary

- [x] Run the following from the S06 worktree. Observed: exact old/new payload equality, unchanged approved digest from the existing verifier, and a single outside-payload erratum.

```powershell
$ErrorActionPreference = 'Stop'
$path = 'docs/devops/adr/ADR-CRM-R00-RELEASE-AUTHORITY.md'
$before = (git show ('HEAD:' + $path)) -join "`n"
$after = (Get-Content -LiteralPath $path) -join "`n"
$pattern = '(?s)<!-- release-decision-payload:start -->(.*?)<!-- release-decision-payload:end -->'
$oldPayload = [regex]::Match($before, $pattern).Groups[1].Value
$newPayload = [regex]::Match($after, $pattern).Groups[1].Value
if ([string]::IsNullOrWhiteSpace($oldPayload) -or $oldPayload -cne $newPayload) { throw 'R00 approved payload changed.' }
$heading = '## P10 S06 Cloudflare R2 勘误（2026-09-07，不进入私有 R00 载荷）'
if ([regex]::Matches($after, [regex]::Escape($heading)).Count -ne 1 -or
    $after.IndexOf($heading, [StringComparison]::Ordinal) -lt $after.IndexOf('<!-- release-decision-payload:end -->', [StringComparison]::Ordinal)) { throw 'Erratum must occur once outside the approved payload.' }
./tools/Test-CrmSaasPublicContract.ps1
if ($LASTEXITCODE -ne 0) { throw 'Existing public contract verification failed.' }
git diff --check
if ($LASTEXITCODE -ne 0) { throw 'Whitespace validation failed.' }
git diff --exit-code HEAD -- .github/workflows scripts deploy
if ($LASTEXITCODE -ne 0) { throw 'Legacy execution paths changed.' }
'PASS: documentation-only erratum and unchanged R00/legacy boundaries.'
```

- [x] Review the complete two-file diff, source attribution, retained No-Go boundaries and absence of secret/machine-specific data. Keep the work in the S06 task branch; no R2/OCI operation is part of this step.
- [ ] Include these named files in the later S06 scoped commit and full PR/main verification. This bounded document check does not complete S06 or P10.

## Plan self-review

The text implements the approved narrow erratum and explicitly preserves the private approval digest, WMS/Shadow gates and non-deployable lane. It distinguishes implemented conditional writes from unimplemented S3 APIs and from actual formal acceptance. No new code behavior is introduced, so existing digest verification and exact-payload comparison cover this documentation-only change.
