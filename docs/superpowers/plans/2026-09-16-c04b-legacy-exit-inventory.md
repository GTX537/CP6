# C04B 模拟开发验收与旧模型退出计划

2026-09-16 用户因没有真实数据，明确允许修改交付条件。原仅盘点方案已扩展为本任务完整 C04B 开发交付；见 [v2 决定](../../crm/C04B-SIMULATED-ACCEPTANCE.md)。独立任务分支执行，整项集中审查，旧成功结果按原范围复用。

- [x] 固定20表的提交绑定引用盘点工具与16项测试；基线准确拒绝8个仍持有旧模型的生产文件。
- [x] 移除旧实体/DbSet/映射/状态机和当前快照34个旧类型块，保留原历史迁移及测试夹具，C01/C02/C03身份与ERP关联不变。
- [x] 增加前向退休标记迁移；无表/数据操作，回退拒绝。4项实际程序集/模型检查通过。
- [x] 独占本机SQL库构造20条模拟记录，执行标记迁移，验证20表记录/列元数据不变、身份/投影/ERP读写和租户隔离；1/1通过，0跳过。不是历史全链重放。
- [x] 受影响16项历史CRM测试通过，Core API及必要依赖编译通过；保留红灯与修复证据。
- [ ] 绑定源码提交生成退出零引用清单，独立发布目录/端口实际启动并HTTP检查，记录发布文件摘要和临时库清理。
- [ ] 双仓库四份状态文档、v2详细进度及证据归档，集中差异审查；每次远端变更前检查Actions触发，正常PR合并并核验远端main包含性。

五个里程碑仍各20%。C04B全部条件交付后由80%达到100%开发验收（模拟数据）；生产验收没有被宣称完成。原Written目标、来源保护、日历和429文件运行包不替换；物理DROP和真实用户采用另行验收，不纳入本轮开发分母。
