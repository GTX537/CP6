# CRM V1 PRD protected-base bootstrap proof

This file exists only on an unmerged audit branch. Its pull request proves that
the required `crm-v1-prd` context validates an exact candidate head with the
validator checked out from the protected `main` branch.

The pull request must be closed after the context succeeds. This file is not
part of the CRM product requirements and must not be merged into `main`.
