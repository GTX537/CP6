# M-ERP T6 任务简报：测试补网(Quotation 报价计算 / Order 建单算价)

## 背景与位置
M-ERP 横切接线波末任务(计划原文: 「T6 测试补网(审计 T5/#6 零→有): QuotationService 报价计算、OrderService 建单算价核心用例」)。同型先例=M-WMS T7(commit 653a7fc): 5 用例绑定真实计算,服务算错会真红,不重复既有覆盖,零生产改动。先 `git show 653a7fc` 学样。

## 需求
1. 先盘点既有覆盖: grep CP6.Tests 中 Quotation/Order 相关既有测试,新用例不得与其重复(报告列出盘点结论)。
2. QuotationService 报价计算核心用例: 找到其计算主路径(单价/数量/金额/折算等),写「给定输入→手算期望值」的真值断言用例,覆盖至少一条正常路径+一条边界(如零数量/空明细/舍入)。
3. OrderService 建单算价核心用例: 建单主路径的价格来源/金额计算/明细汇总真值断言,同样正常+边界。
4. 断言必须是**手算期望值**(服务算错会红),禁止「调用后把服务输出当期望值」的套套逻辑。
5. 零生产代码改动;若发现真实缺陷,不修——记入报告 concerns(交主控裁决)。
6. 用例数量以覆盖质量为准(M-WMS T7 为 5 用例量级),勿凑数。

## 验证与提交
- 焦点 filter 全绿;全量测试(以 T5 后基线为准)不许跌。
- 单 commit 即 push。

## 报告契约
详细报告写入 `C:\CP6\.superpowers\sdd\erp-t6-report.md`(既有覆盖盘点+逐用例「输入→手算期望」演算+测试输出)。回复只返回: 状态、commit sha、一行测试结论、concerns、报告路径(15 行内)。
